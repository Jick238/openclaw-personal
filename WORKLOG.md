# OpenClaw customization worklog

## 2026-09-09 Guest Chat Mode runtime deployment

- Current checkout bundle was transferred through `ssh hicks-companion` to Windows Temp and executed in Ubuntu-24.04 WSL with `scripts/hicks-deploy-runtime.mjs`, install root `/usr/lib/node_modules/openclaw`, workspace `/home/clawd/.openclaw/obsidian-operating-base`, and state root `/home/clawd/.openclaw`.
- Transaction completed with retained install/reference backups; config, memory, proxy, and SQLite state were outside the cutover root. Cold restart was issued through `openclaw-gateway.service`; post-restart readiness was checked at `/readyz` and the installed runtime resolves via `/usr/bin/openclaw` to `/usr/lib/node_modules/openclaw/openclaw.mjs`.
- Guest Chat Mode marker `supports_guest_queries` is present in installed `dist`; canonical `hicks-reference` is present. **READY_FOR_USER_TEST**. No Telegram send, Computer Use, config, or schema mutation was performed.

## 2026-09-08 Companion Workboard async-config load repair

- Reproduced the source race in `ui/src/pages/workboard/workboard-page.ts`: a connected page can render while `runtimeConfig.ensureLoaded()` is still pending, so `pluginEnabled()` is `null` and the Workboard loader is skipped. The prior async config completion did not itself schedule a page update; the page could remain on the empty pre-load projection. This matches the live Companion window where no `workboard.cards.list` or `workboard.boards.list` request appeared despite authoritative cards.
- The page now schedules one render after that authoritative config load succeeds, guarded by the same context instance. This lets the existing `pluginEnabled===true` path configure the current Gateway client and call `loadWorkboard`; it adds no polling, fallback Gateway, compatibility branch, config option, or persistence surface. The test `ui/src/pages/workboard/workboard-page.test.ts` covers config arriving asynchronously before the first Workboard load.
- Validation: focused Workboard page suite passed **13/13**; `oxfmt --check`, targeted `oxlint`, and `git diff --check` passed. The full Playwright Workboard E2E lane was attempted but is blocked on the local Chromium executable being absent (`OPENCLAW_UI_E2E_ALLOW_MISSING_CHROMIUM` was not used). No Computer Use, Telegram interaction, config, or SQLite mutation was performed.

## 2026-09-08 passive ordinary-DM / Companion Workboard trace

- Read-only capture through canonical `ssh hicks-companion` -> `wsl.exe -d Ubuntu-24.04` correlated Telegram updates `182010672` through `182010679` (23:21:36-23:25:23 +03) for the owner DM lane. Every ingress event was spooled and completed; visible outbound text deliveries were recorded as message ids `108`, `110`, `112`, `114`, `116`, `118`, `120`, and `123`. No Telegram interaction was performed by this audit.
- The authoritative main transcript session `agent:main:telegram:direct:351358741` contains the corresponding ordinary user turns (bounded lengths only; content omitted). For these latest turns there is no `hicks_delegate` tool call and no new `task_runs` row. Older `hicks_delegate` calls are present; one read-only result at the earlier admission boundary was `status=error` because owner `hicks-orchestrator` already had active Workboard work. The deployed plugin is enabled/activated and its tool surface includes `hicks_delegate`, so this trace is Front answering without delegation rather than a missing tool or admission rejection for the latest turns.
- Canonical read-only `openclaw workboard list --board hicks-front --json` returned four cards at capture time: two blocked cards from the earlier active work and a completed parent with two completed children from the 23:23 run. The shared state DB had six task rows, with no task row corresponding to the 23:25 ordinary DM. Therefore the Companion screenshot showing all Workboard columns/counts zero does not match authoritative state.
- Companion WebSocket activity on the same Gateway connection around 23:25 included `plugins.list`, `chat.startup`, and session calls, but no `workboard.cards.list` or `workboard.boards.list` request. The UI loader requests `workboard.cards.list` only after an authenticated connection and enabled plugin are established (`ui/src/lib/workboard/loading.ts:77-112`), while the page starts that sync from `ui/src/pages/workboard/workboard-page.ts:200-233`. The observed zero board is consequently a stale/unloaded projection (or a Companion connection to another Gateway owner), not an empty Workboard authority. No source fix was made because Front routing is the active overlapping rewrite; no config, schema, state, or Telegram mutation was made.

## 2026-09-08 Luna passive inline trace and cleanup

- Passive owner capture through `ssh hicks-companion` → `wsl.exe -d Ubuntu-24.04` found inline ingress update ids `182010639` (19:02:42.152 +03), `182010640` (19:02:44.391), and `182010641` (19:02:54.052). Two redacted timing records show `inline_placeholder_answered`: query `1509074303177649031`, `ingressAgeMs=17`, `answerMs=484`; query `1509074302254421409`, `ingressAgeMs=5`, `answerMs=120`. Both recorded answers are below the 13 s bound. No `answerInlineQuery` invocation/result/error, `chosen_inline_result`, Front response, or final DM evidence appears in the live journal. The second query committed at 19:02:54.755 with task id `144bd5d3-…`, about 10.2 s after its placeholder.
- Source owner confirms the delay: `extensions/telegram/src/bot-handlers.inline.ts:212-281` admits only from `callback_query:data` or `chosen_inline_result`; `:357-392` always returns a placeholder with `✅ Выполнить` callback. There is no automatic local/actionable classifier or automatic background admission after query answer. This explains the delayed commit and is a fast-front source-lane fix, left untouched here.
- Cleanup used the canonical CLI/API. A redacted cron snapshot was backed up under `/tmp/hicks-cron-cleanup-20260908/cron-before.json`, and the live config under `/tmp/hicks-cron-cleanup-20260908-openclaw.json`; the user-owned memory promotion job was initially removed. System-owned monitor ownership was traced to `src/cron/heartbeat-monitor.ts`, `src/gateway/server-cron-skill-review-jobs.ts`, and `extensions/memory-core/src/dreaming.ts`; using their existing config controls, `agents.entries.main.heartbeat.every=0m`, `skills.workshop.autonomous.mode=off`, and `plugins.entries.memory-core.config.dreaming.enabled=false` were set through `openclaw config set`. After restart through the owning system service, canonical `cron list --json` reports `jobs=[]`, `total=0`; no bypass or direct store mutation was used. Both `agent:main:main:heartbeat` and the required `agent:main:main` handle were reset through `sessions.reset`; their post-reset entries have zero token counts and no active runs. Workboard cards were deleted through `workboard.cards.delete`; `hicks-front` now reports zero cards. Post-restart `/readyz` is `ready:true`, event loop healthy, Telegram lifecycle `ready`/connected, and `health --json` is `ok:true`.
- Canonical memory hashes/counts were captured as `workspace/MEMORY.md` SHA-256 `b5a15d…b6d0` (4 lines/28 words/206 bytes) and `obsidian-operating-base/MEMORY.md` SHA-256 `24560c…3817` (56/1168/10598); no memory write was performed. Memory-core dreaming scheduling was disabled through its owner config; memory contents and search data were unchanged. The one new failed inline task and its one `task_delivery_state` dependent row were removed first through a bounded stopped-Gateway `BEGIN IMMEDIATE` transaction after fresh SQLite backups under `/home/clawd/.openclaw/backups/openclaw-cleanup-20260908-{state,main,orch}.sqlite`. The subsequent full authoritative registry showed five historical terminal non-memory task rows (three subagent, two heartbeat automation) and three delivery rows; a fresh pre-purge backup `/home/clawd/.openclaw/backups/openclaw-cleanup-20260908-pre-all-tasks.sqlite` was captured, then all five task rows and three dependents were removed in a second FK-enabled transaction. Both transactions passed exact bounded checks, `foreign_key_check`, and `integrity_check=ok`. After cold restart, `tasks.list` reports `count=0`, cron remains `total=0`, Workboard remains empty, and service/Telegram readiness remains healthy. Mandatory fresh main/heartbeat sessions and all memory data were preserved.
- Canonical memory hashes/counts were captured as `workspace/MEMORY.md` SHA-256 `b5a15d…b6d0` (4 lines/28 words/206 bytes) and `obsidian-operating-base/MEMORY.md` SHA-256 `24560c…3817` (56/1168/10598); no memory write was performed. Memory-core dreaming scheduling was disabled through its owner config; memory contents and search data were unchanged. The one new failed inline task and its one `task_delivery_state` dependent row were removed first through a bounded stopped-Gateway `BEGIN IMMEDIATE` transaction after fresh SQLite backups under `/home/clawd/.openclaw/backups/openclaw-cleanup-20260908-{state,main,orch}.sqlite`. The subsequent full authoritative registry showed five historical terminal non-memory task rows (three subagent, two heartbeat automation) and three delivery rows; a fresh pre-purge backup `/home/clawd/.openclaw/backups/openclaw-cleanup-20260908-pre-all-tasks.sqlite` was captured, then all five task rows and three dependents were removed in a second FK-enabled transaction. Both transactions passed exact bounded checks, `foreign_key_check`, and `integrity_check=ok`. Four historical non-mandatory sessions were removed through canonical `sessions.delete`; OpenClaw retained their timestamped compressed `.jsonl.deleted` archives for recovery. After cold restart, authoritative counts are `sessions=2` (only `agent:main:main` and `agent:main:main:heartbeat`), `tasks=0`, `cron_total=0`, and `workboard_cards=0`; `/readyz` is ready with no failing components. Mandatory fresh main/heartbeat sessions and all memory data were preserved.

## 2026-09-08 Hicks architecture requirement gap matrix (Luna, read-only)

This matrix is based on the current checkout, focused tests, the canonical plan/runbook, and the latest live observations. It does not treat prompt text or service health as proof of an end-to-end behavior.

| Requirement                                     | Verdict                                   | Current evidence and exact gap                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----------------------------------------------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Fast Front dialogue and fast memory             | **MISSING**                               | Slash commands have a native fast path (`src/auto-reply/reply/get-reply.ts:491-515`), but ordinary greeting, small talk, and simple recall still enter the model turn. Bootstrap caching (`src/agents/bootstrap-cache.ts:44-67`) is context preparation, not a pre-model local disposition/answer boundary.                                                                                                                                                                                  |
| Separate Orchestrator                           | **SOURCE PASS / LIVE E2E UNKNOWN**        | `extensions/workboard/src/front-admission.ts:88-108,245-327` creates a durable parent and native `hicks-orchestrator` child; policy asset denies `hicks_delegate` to the Orchestrator. Fresh depth-0→1→2 completion evidence is still absent.                                                                                                                                                                                                                                                |
| Workboard authority                             | **SOURCE PASS / LIVE PROJECTION UNKNOWN** | Parent/child lifecycle, claims, launch CAS, proof and terminal validation are persisted by `extensions/workboard/src/store.ts` and `store-workflow.ts`; fresh Companion parity and terminal-card evidence remain unmeasured.                                                                                                                                                                                                                                                                 |
| Parallel native Luna workers                    | **SOURCE PARTIAL / LIVE FAIL-INCOMPLETE** | Orchestrator prompt requires `independentChildren=true` and parallel native spawn (`extensions/workboard/src/front-admission.ts:105`); dispatcher protects capacity with a per-store promise chain (`extensions/workboard/src/dispatcher.ts:286-307`) and starts its own selected cards sequentially (`:345`). This preserves ordered admission but needs a real multi-lane overlap trace; latest owner A/B was blocked by active-task contention before the slot-owner repair was deployed. |
| Ordered admission without serial execution      | **SOURCE PARTIAL**                        | Idempotent parent admission and requester-bound card slots exist (`front-admission.ts:245-327`, `store-constants.ts:48-68`). The dispatcher queue is intentionally serialized for capacity snapshots (`dispatcher.ts:289-299`), so acceptance must show that only admission/claim ordering is serialized while independent lanes overlap. No fresh proof exists.                                                                                                                             |
| Merge, validation, and final delivery ownership | **SOURCE PASS / LIVE UNKNOWN**            | Orchestrator prompt assigns Workboard reconciliation and explicitly forbids owner messaging (`front-admission.ts:88-108`); Front control and completion seams are typed. Exactly one validated final Telegram DM after worker completion has not been observed in the current acceptance lane.                                                                                                                                                                                               |
| Long-term memory                                | **PARTIAL**                               | Canonical bootstrap and asynchronous memory preparation exist (`src/agents/bootstrap-cache.ts:44-67`, `src/plugins/memory-state.ts:259-320`), but no bounded Front current-task/profile projection or live correction/restart/recall acceptance exists. Do not add a duplicate file or SQLite store.                                                                                                                                                                                         |
| Full machine execution planes                   | **SOURCE PASS / RECOVERY MISSING**        | Plan routes WSL, native Windows, and browser through distinct Gateway owners; fresh read-only node/browser/Companion health is good. Windows reboot, network interruption, WSL restart, stale-worker fencing, and post-recovery reversible fixtures remain unproved.                                                                                                                                                                                                                         |
| Inline background delegation and final DM       | **SOURCE PARTIAL / LIVE BLOCKED**         | Inline placeholder/callback/deadline and bounded correlation are covered by focused Telegram tests; the local runner has no inline-query/chosen-result driver, and no current live inline commit/final-DM trace is claimed.                                                                                                                                                                                                                                                                  |
| Deployment/runtime coherence                    | **BLOCKED**                               | Live recovery audit reports CLI 2026.8.1 reading config written by 2026.8.2 with pending `audit-events-v2`. Safe resolution is to preserve proxy/config/state, obtain explicit approval for the owning upgrade or migration, then run the canonical deployment owner; do not use `doctor --fix` or mutate SQLite/config in this audit.                                                                                                                                                       |

- Live boundary: the reachable `192.168.0.181` observer is Armbian/LAN jump only; it has no Hicks Gateway and is not deployment readiness evidence. The latest owner-provided live state reports Gateway/nodes/browser/Companion health, while reboot/network fault proof and user-visible end-to-end completion remain missing.
- Codex dependency contract checked directly: `../codex/codex-rs/app-server-daemon/src/backend/pid.rs:156-171` uses additive child environment setup, `../codex/codex-rs/app-server-test-client/src/lib.rs:1538-1564` passes environment overrides to the app-server child, and `../codex/codex-rs/features/src/lib.rs:1026-1033` keeps `respect_system_proxy` disabled by default. OpenClaw's managed launch enforcement is therefore source-supported, but does not substitute for fresh live child proof.
- No source, config, SQLite, proxy, service, browser, or Telegram mutation was made by this audit. Targeted validation after this entry: `git diff --check`; focused Workboard/Front tests are the required next local gate before any deployment.

## 2026-09-08 Luna Windows/WSL/Companion read-only lifecycle audit

- Fresh local WSL observer reached `openclaw-gateway.service`: systemd reports `active`, `/readyz` returned `ready:true`, `eventLoop.degraded=false`, and the canonical proxy route remains outside this observer's mutable scope. No Telegram traffic or service mutation was performed.
- Fresh Gateway node inventory reports the approved `Windows Headless` identity `3aa5…07593` connected, with `browser`/`system` capabilities and `lastSeenReason=connect`. Routed `system.which` succeeds for canonical bare names `node`, `powershell`, `wsl`, and `git`, returning the portable/native Windows paths. A request using `.exe` suffixes returned an empty map because the node command resolves the supplied bin names literally; this is caller evidence, not node loss.
- Fresh routed browser read-only checks report `icloud` `running=true`, `cdpReady=true`, `pageReady=true`, attach-only CDP on `18801`; `/tabs` returned the authenticated `https://www.icloud.com/photos/` page titled `Фото iCloud`. No browser mutation occurred.
- Companion state database still passes `PRAGMA integrity_check=ok`; `device_identities` is the canonical `STRICT` table with its index. This confirms the prior persisted identity schema repair remains intact, but is not a fresh UI/control-ping proof because this lane forbids Computer Use and Telegram actions.
- The local CLI binary is 2026.8.1 while the local config reports it was written by 2026.8.2; read-only commands emit a pending `audit-events-v2` state migration warning. No `doctor --fix`, SQLite migration, or config change was attempted because schema/persistent-state changes require explicit approval and this mismatch is outside the Windows recovery owner.
- Windows fault injection, network interruption, WSL restart, Windows reboot, and duplicate/stale fencing were not performed in this read-only lane. Existing worklog evidence covers bounded node stop→Task Scheduler relaunch and Gateway restart repeats; the remaining reboot/network acceptance must stay explicitly unclaimed until measured.

## 2026-09-08 passive Luna acceptance capture

- Reviewed the current Hicks plan, acceptance runbook, Telegram ingress timing
  seam, Front admission/control, Workboard gateway/store projections, dispatcher
  lifecycle, and the neighboring Codex managed app-server contract. Existing
  source evidence is sufficient for passive capture without Telegram mutation:
  ingress logs carry update receipt timestamps and update ids; Workboard cards
  carry parent/child ids, run/session links, status events, launch acceptance,
  heartbeats, attempts, terminal state, and bounded worker logs; inline logs
  carry `queryId`, `resultId`, `ingressAgeMs`, `answerMs`, deadline, and commit
  outcome. No config or SQLite schema change is required.
- Added section 8a to `HICKS_ACCEPTANCE_RUNBOOK.md` with exact before/after
  read-only capture commands, bounded JSONL/journal and Workboard snapshots,
  per-card run collection, acceptance predicates, and two short read-only A/B
  owner prompts. The operator sends those prompts and any inline query manually;
  this lane sends no Telegram traffic and does not use Telegram Desktop,
  Telegram2, Computer Use, or the userbot.
- Live status boundary: `ssh clawd@192.168.0.181` reached `armbian`, where only
  the Home Assistant tunnel unit was present, no `openclaw-gateway.service`
  existed, and `127.0.0.1:18789` refused connections. This host is therefore
  only the LAN jump observer and cannot establish Hicks deployment readiness.
  No live mutation was attempted and proxy/VLESS state was untouched.
- Validation: runbook/worklog edits pass `git diff --check`; no production,
  config, state, or schema files were modified. Targeted source review confirms
  the capture fields described above match current code paths.

## 2026-09-07 Hicks Front control owner-boundary repair (Luna)

- The Front `/status`, `/tasks`, `/steer`, and `/stop` bridge no longer reads channel, owner, authorization, agent, session, or delivery fields from the action payload. Core supplies a host-authenticated command context; direct Gateway `plugins.sessionAction` calls therefore cannot forge Hicks owner control and return `not_applicable`.
- Mutating controls revalidate the exact `main` Telegram private delivery session and account immediately before steering or aborting, and again before Workboard stop commit. Workboard card and execution identity remain bound to the requester session; stale delivery and run changes fail closed.
- Focused proof: session-action, Front control, and Gateway contract suites pass (41 tests); core/extensions tsgo, oxfmt, oxlint, and `git diff --check` pass. No live deploy or Telegram send was performed.

## 2026-09-07 Hicks reference bundle and role-contract repair (Luna)

- The deployment audit found that the checked-in `customization/hicks/OWNER_POLICY.md`, `HICKS_ARCHITECTURE_PLAN.md`, and `WORKLOG.md` were not copied by the full-bundle runtime owner. A fresh live session could therefore receive an older manually edited workspace policy while the source and evidence had moved on. The canonical target workspace remains `obsidian-operating-base`; the historical `workspace` tree is never selected automatically.
- `scripts/hicks-deploy-runtime.mjs` now requires an explicit live workspace root and stages the three versioned files under `hicks-reference/`, with SHA-256 manifest, same-filesystem rename, retained backup, failed bundle, and coordinated rollback with the runtime install. Missing workspace, source file, symlink, or occupied rollback destination fails before service stop. `scripts/update-gateway.sh` passes `OPENCLAW_UPDATE_WORKSPACE_ROOT` and refuses a full-bundle deployment without it.
- `src/agents/system-prompt.ts` keeps the owner policy bounded and adds only relative references to the full plan/worklog; it does not inject their contents. The role contract now says explicitly that Telegram ingress is Front and internal sessions are Hicks Orchestrator, removing the stale wording that labeled every spawned session as Telegram Front. `src/agents/system-prompt.test.ts` covers fresh Front/internal-Orchestrator prompt assembly and stale-clause absence.
- Focused proof: deployment transaction 4/4 and system-prompt 134/134; oxfmt and `git diff --check` passed. No live deploy or Telegram send was performed. Live acceptance still requires the owner to supply the configured `obsidian-operating-base` workspace root, run the transaction, then verify a fresh main and orchestrator prompt plus Companion/Telegram behavior.

Started 2026-09-06 after the historical worklog became unavailable in the active project view. Historical source remains available at `/Users/wholewnewpea/Movies/openclaw-backups/20260831-hicks-pre-upgrade/ignored-local-artifacts/PERSONAL_DEVELOPMENT_LOG.md`.

## Scope and acceptance

- Persistent Telegram coordinator: native OpenClaw `sessions_spawn(runtime="subagent")`, parallel A/B work, prompt C remains responsive, targeted cancellation, exactly-once result delivery, recovery after restart.
- Existing authenticated Windows iCloud browser session: read-only status/tabs/snapshot and photo metadata/preview path before any mutation.
- Reliable host interaction and recovery: stable node identity, explicit Windows versus WSL execution context, browser endpoint ownership, and actionable failure reporting.

## 2026-09-06 audit evidence

- Current source has native spawn, durable subagent registry, Task Flow persistence, Telegram ingress spool, queue modes `steer/followup/collect/interrupt`, and requester-targeted abort. These are source-level capabilities; the end-to-end Telegram scenario is not yet live-proven.
- Installed runtime is OpenClaw 2026.8.1. Live config pins exec and browser routing to Windows Headless node `3aa5…07593` (identifier abbreviated here); node status reports that node paired and connected.
- The same live config points `windows-visible` CDP to `172.20.0.1:9222`; a read-only TCP probe issued inside remote Ubuntu-24.04 WSL through `hicks-companion` returned connection refused. This proves that endpoint was unavailable from that WSL namespace at probe time; it does not prove Windows exec is generally unavailable, nor that `127.0.0.1:18801` is comparable because it belongs to another owner namespace/profile. SSH/WSL in this route required the absolute Windows `wsl.exe` path. Disabled sandbox does not establish reachability.
- Installed dist has no active `operating_base_*` or commitments tool. `openclaw commitments --json` is unknown. Vault `OPERATING_BASE.md` and decision notes still describe those retired contracts, so they can mislead the agent.
- Existing iCloud read-only audit report recorded authenticated metadata access on 2026-08-09, but it is historical; the current CDP endpoint refusal prevents fresh proof.

## Changes and verification

- This file is the only new project artifact. No production files, live services, browser state, photos, accounts, or devices were changed.
- Verification performed read-only: source inspection, installed-version/config inspection, node status, WSL marker attempts, CDP `/json/version` and `/json/list` probes from the remote WSL namespace against the configured `windows-visible` endpoint, and historical report review. No routed browser tool call was claimed as live proof; no secrets were printed.

## Unknowns / next tasks

- Reproduce the A/B/C Telegram scenario through the real ingress and record run IDs, queue admission, cancellation scope, and one terminal delivery. Verify native subagent runtime rather than ACP/Codex-internal spawning.
- Trace why the pinned Windows node can be connected while its configured CDP endpoint refuses: endpoint owner/profile, Windows interactive versus headless process, lifecycle/reconnect owner, and whether `icloud` 127.0.0.1:18801 is the actual supported route.
- After endpoint recovery, perform only browser status/tabs/snapshot and the existing metadata audit; separate auth/2FA/manual blockers from route/tool-policy failures.

## 2026-09-06 source repair decision

- Inspected the browser owner path under `extensions/browser/src/gateway/browser-request.ts`, `browser-node-routing.ts`, `node-host/invoke-browser.ts`, `browser-node-fallback.ts`, and the scoped extension rules. The configured node proxy already preserves node identity, classifies pre-dispatch host-unavailable errors, and includes redacted CDP status on timeout; automatic Gateway fallback is intentionally disabled when a node is explicitly configured. The observed refusal came from an external attach-only Windows CDP listener, not from a demonstrated source invariant failure.
- No source patch was justified without a reproduction through the routed browser request path. Adding fallback, rotating node IDs, or starting a second browser service would hide the owner/configuration failure and could route a browser action to the wrong host. No production LOC or tests were changed. The exact next repair belongs to the live Windows browser-process/profile lifecycle owner: restore the configured endpoint or change the reviewed route, then rerun browser status/tabs/snapshot.
- Verification note: the targeted browser Vitest command was started, but the two broad suites did not produce completion within the bounded local run and were interrupted; no test result is claimed. The no-patch decision is based on source ownership and the absence of a routed-path reproduction.

## 2026-09-06 native coordinator lifecycle review

- Source owners reviewed: `src/agents/tools/sessions-spawn-tool.ts` (native `runtime="subagent"` launch), `src/agents/subagents/registry/subagent-registry-run-manager.ts` and `subagent-registry-restart-recovery.ts` (durable child ownership/restart), `src/agents/subagents/completion/subagent-completion-delivery.ts` (completion delivery redrive), `src/auto-reply/reply/get-reply-run-queue.ts` and `commands-steer.ts` (active turn queue/steering), `commands-session-abort.ts` (requester-targeted cancellation), and Telegram `bot-message.ts`/ingress spool (durable ingress/adoption).
- Existing tests already cover the requested invariants: 8 test files, 98 tests passed. Coverage includes atomic linked owner rollback, expired orphan generations, canonical payload reload, sibling isolation for retry/dismiss, exactly-once ended hooks, restart recovery reservation/settlement, stale-row dedupe, nested parallel counting, and requester run-window scoping.
- No source invariant gap was reproducible from current code/tests. Production LOC: 0; no coordinator or scheduler patch made. Remaining proof is live: real Telegram A/B overlap, prompt C admission, A-only cancel, exactly-once B delivery, and native runtime trace after a Gateway restart.

## 2026-09-06 live Windows browser owner inspection

- Read-only PowerShell inspection found no listener on `9222`. The configured `windows-visible` endpoint is therefore not owned by a current Windows process.
- A Windows Chrome process (PID intentionally omitted) owns `127.0.0.1:18801`; its command line uses the OpenClaw ChromeCDP profile and opens iCloud Photos. Windows-local `/json/version` returned Chrome 152 metadata, but `/json/list` contained no usable page target at probe time.
- A Windows OpenClaw node process was observed with display name `Windows Interactive`, serving `127.0.0.1:18789`; on a later read-only listener check that process had exited and the port was owned by `wslrelay.exe`. Gateway `nodes status` simultaneously reported only `Windows Headless` connected. This is evidence of an unstable/mismatched Windows node lifecycle, not a source browser routing bug.
- No repair was applied: recovering the interactive node or changing the configured node requires a Windows service/process restart or pairing/config change, which is outside this read-only lane. Exact live owner action: inspect the Windows task/service that owns the `Windows Interactive` node, keep it running and paired, then align Gateway `gateway.nodes.browser.node` with the actually connected node and verify `browser status`, `tabs`, and iCloud snapshot. Do not use `9222` until a listener exists.

## 2026-09-06 authorized live repair

- Before-state was captured under `/tmp/hicks-before-20260906/`: live `openclaw.json`, the XML for `OpenClawNodeStable`, `OpenClawICloudBrowserStable`, and disabled `OpenClaw Node`, plus the original `openclaw-node-service-20260828.ps1`. Secrets were not printed or copied into the project.
- Root cause found in the Windows node owner script: it gated node launch on `Test-GatewayPort`, which probed `127.0.0.1:18789` — the node's own listener — and therefore prevented recovery after node loss. The script also had no reason to wait for this port before launching.
- Repaired the owner script in place at `C:\Users\clawadmin\.openclaw\openclaw-node-service-20260828.ps1`: removed the self-port gate, retained the mutex, duplicate termination, exponential early-exit backoff, and logging. Remote SHA-256 matches the locally prepared bounded repair `E24B7E950B9D527AE56CB2839572A366A4DB267F2ECE934B7F44D15D69F71278`.
- Restarted only the owned `OpenClawNodeStable` task/node process. Induced a controlled node drop by stopping its node process; the owner relaunched a new node within 8 seconds and logged a second `launching node`. This proves local process recovery, not Gateway pairing or full browser recovery.
- Remaining live blocker: the restarted `Windows Interactive` node is not present as connected in Gateway `nodes status`; status shows the old Windows Headless identity offline. Pairing/identity or Gateway binding still requires a separate owner-level repair. Rollback is to restore `/tmp/hicks-before-20260906/openclaw-node-service.ps1` and restart `OpenClawNodeStable`.

## 2026-09-06 Gateway route repair

- Pairing inspection found the current Windows `node.cmd` route was `--host 127.0.0.1 --port 18789`, while the historical route backup `node.cmd.bak-route-20260827` uses the established public TLS Gateway `myhajick238.duckdns.org:18790`. The node was demonstrably self-looping to its own 18789 listener, so the binding could not reach the intended Gateway.
- Backed up the current file to `C:\Users\clawadmin\.openclaw\node.cmd.pre-route-repair-20260906`, restored only the existing TLS route while preserving the current token, and restarted `OpenClawNodeStable`.
- After repair, Windows TCP state shows an established connection from the node to `185.60.45.42:18790` (the resolved public route). The local WSL Gateway `nodes status` still reports the old Windows Headless entry offline; this is not claimed as successful pairing because the local status owner may differ from the public TLS Gateway. No pairing approval or identity replacement was fabricated.
- Rollback: restore `node.cmd.pre-route-repair-20260906` and restart `OpenClawNodeStable`. Remaining bounded step: query the Gateway owning `myhajick238.duckdns.org:18790` for the node registry or complete its existing approval flow, then verify routed exec and browser status/tabs/snapshot.

## 2026-09-06 authoritative public Gateway proof

- The local WSL Gateway listens on `0.0.0.0:18789`; its local `nodes status` is a stale/different observer for this route. Using the Gateway token in memory only, the canonical public TLS endpoint `wss://myhajick238.duckdns.org:18790` reported Windows Headless identity `3aa5…` connected.
- Through that authoritative public Gateway, read-only `nodes invoke system.which` returned the Windows portable Node path. Read-only `browser.proxy` status returned `profile=icloud`, `driver=openclaw`, `transport=cdp`, `cdpReady=true`, `pageReady=true`, `cdpPort=18801`, attach-only. `/tabs` returned the existing `https://www.icloud.com/photos/` tab titled `Фото iCloud`; `/snapshot` returned the authenticated Photos UI with Apple ID control, album tree, and media navigation. No photo/account mutation occurred.
- This proves the route repair restored authoritative node and browser access. The local observer must not be used as acceptance evidence for this public TLS deployment. The prior controlled stop/relaunch proof remains valid for the repaired owner script; no further Gateway restart was performed.

## 2026-09-06 Mac Companion lane paused Windows work

- Windows binding/browser acceptance is now recorded above. Further Windows changes are paused while inspecting the local macOS Companion only.

## 2026-09-06 Mac Companion identity repair

- Read-only local inspection found `/Users/wholewnewpea/.openclaw/openclaw.json` already configured for direct remote `wss://myhajick238.duckdns.org:18790` with the existing remote token source; the previous backup used SSH transport and `ws://127.0.0.1:18789`. OpenClaw.app was installed and running, with the remote chat UI visible.
- Companion's reported error `device_identities must be a STRICT table` was reproduced against `/Users/wholewnewpea/.openclaw/state/openclaw.sqlite`: the table existed with the canonical columns but without `STRICT`; integrity check was otherwise `ok`. Source owner is `apps/shared/OpenClawKit/Sources/OpenClawKit/DeviceIdentitySQLiteStore.swift`, which validates canonical strict tables before loading/creating identity.
- Before mutation, created `/tmp/hicks-companion-before-20260906/openclaw.sqlite` using SQLite backup and copied `openclaw.json`. Quit only OpenClaw.app, rebuilt `device_identities` as the same canonical `STRICT` table and recreated `idx_device_identities_device`, preserving all existing rows (there were none), then verified `PRAGMA integrity_check = ok` and exact schema.
- Relaunched OpenClaw.app. UI now visibly loads `myhajick238.duckdns.org:18790/chat/main` with sessions, Workboard, model controls, and message composer; the persisted-identity schema error is no longer blocking the Companion UI. No Gateway token, remote config, Windows node, or photos were changed.
- Rollback: quit OpenClaw.app and restore `/tmp/hicks-companion-before-20260906/openclaw.sqlite` plus `/tmp/hicks-companion-before-20260906/openclaw.json`. Remaining proof: send a harmless control ping or message only if separately authorized; current UI load confirms connection surface but not a user-visible message round trip.

## 2026-09-06 Companion reconnect follow-up

- After a second authorized quit/relaunch of OpenClaw.app, the accessibility tree still shows `Dashboard reconnecting` and `The selected Gateway changed. Waiting for a fresh authenticated connection.` No token, Gateway, or remote service was changed. This is not a successful final Companion connection proof.
- The repaired local `device_identities` table is `STRICT`, has the expected index, passes SQLite integrity, and contains zero rows. The remaining owner boundary is the app's remote-device identity/pairing handshake after a Gateway selection change; relaunch alone did not complete it. Exact single operator action if the app presents it: approve the pending Companion device in the Gateway's device-pairing UI, then relaunch/refresh the app. No approval prompt was exposed in the current accessibility tree, so no approval was attempted.

## 2026-09-06 Telegram inline mode lane

- Installed/live OpenClaw 2026.8.1 reports the configured Telegram account `ready`, polling, and `getMe` capability `supportsInlineQueries=true` for `hickss_bot`; the probe was redacted and printed no token. This proves Bot API/account capability and transport health only.
- Source `extensions/telegram/src/allowed-updates.ts:9-17` inherits grammY default update types, which includes `inline_query` and `chosen_inline_result`; polling/webhook pass that list at `extensions/telegram/src/monitor.ts:48` and `extensions/telegram/src/telegram-ingress-worker.runtime.ts:263`. However `extensions/telegram/src/bot-handlers.inbound-pipeline.ts:425-436` registers only message, edited-message, channel-post, and edited-channel-post handlers. There is no `inline_query`/`chosen_inline_result` handler, answer path, result schema, or model/tool routing in source or installed dist. Inline buttons are a separate implemented feature and do not provide Telegram inline mode.
- Therefore no existing config switch can enable inline query answers. No source/config patch was made: adding it is a new product surface requiring a routing/result contract and live Telegram proof, not a safe setup toggle. BotFather must still be configured externally with `/setinline` for the bot; prepare that as a manual operator step only, never send it from OpenClaw. Targeted proof: `pnpm vitest run extensions/telegram/src/allowed-updates.test.ts` passed (1 file, 1 test).

## 2026-09-06 Telegram inline implementation assessment

- The existing native path is message-shaped end to end: `createTelegramInboundPipeline` → `processInboundMessage` → `createTelegramMessageProcessor` → `dispatchTelegramMessage`, where the latter owns chat delivery, streaming previews, reply threading, durable transcript/adoption, and completion status. An inline query has no `Message`/`chat_id`/`message_id`; its result must be returned by `answerInlineQuery` and may be selected into an arbitrary chat later.
- A direct adapter that fabricates a Telegram message would violate the existing durable ingress, session identity, dedupe, and delivery contracts. Reusing `processMessage` would send a normal chat reply instead of returning an inline result; intercepting outbound sends would create a second delivery path. A deterministic echo/help result would technically answer queries but would not provide useful Hicks orchestration and would falsely imply model-backed inline mode.
- Consequently no speculative source patch was made in this pass. The concrete implementation seam is a new typed inline coordinator owned by the Telegram plugin: authorize the inline sender, derive a stable inline session key from account + sender + query context, enqueue through the native Task Flow/session coordinator, collect a bounded final text result without normal chat delivery, map it to a Telegram `InlineQueryResultArticle`, and answer exactly once; `chosen_inline_result` needs a separate persistence/reply-context contract before enabling it. Focused tests must cover empty query, authorized query, model failure, timeout, duplicate query id, and exactly-once `answerInlineQuery`.
- BotFather `/setinline` remains a manual external prerequisite and was not sent. Existing live probe still reports `supportsInlineQueries=true`; this is Bot API capability, not implementation proof.

## 2026-09-06 inline seam implementation checkpoint

- Rechecked the public plugin SDK before editing. It exposes active-run steering (`queueAgentHarnessMessage`) and runtime helpers, but no plugin-safe API that starts a fresh native agent turn and returns its final text without channel delivery. The underlying embedded-run internals are explicitly marked internal/deprecated for plugin use; importing them from Telegram would cross the core/plugin boundary and create an unsupported second delivery path.
- Per the repair boundary, stopped before speculative code. The exact missing owner is a core/plugin SDK contract for “admit inline request → run through canonical native coordinator → collect bounded final result → let Telegram answerInlineQuery exactly once”. Once that seam exists, Telegram can add its typed adapter and focused tests without fabricating `Message`/`chat_id` or bypassing Task Flow. No production LOC changed in this checkpoint.

## 2026-09-06 inline core seam follow-up

- Rechecked the existing core plugin runtime: `src/plugins/runtime/runtime-embedded-agent.runtime.ts` already exposes `PluginRuntime.agent.runEmbeddedAgent` with exact plugin admission/authority handling, and `RunEmbeddedAgentParams` supports `sourceReplyDeliveryMode`, `disableMessageTool`, `terminalReplyExpectation`, sender/channel/session context, and bounded `payloads` results.
- The remaining missing contract is narrower than a new scheduler but still crosses core ownership: a public result-only wrapper must set the no-channel-delivery policy, collect non-commentary final text, classify empty/error/timeout, and define exactly-once terminal ownership for an external request. Telegram cannot safely duplicate those rules locally. No core API mutation was made in this checkpoint because changing `PluginRuntime` types, runtime registration, and public SDK exports without a complete adapter/test path would leave an uncalled contract and increase production surface without proof.

## 2026-09-06 inline implementation boundary recheck

- The Telegram bot construction path currently forwards only the channel inbound dispatcher into `RegisterTelegramHandlerParams`; it does not carry the owning `PluginRuntime.agent` surface into the bot handler registration. The plugin-local runtime store is intentionally limited to channel/state helpers and is not a substitute for the request-bound plugin runtime scope required by `runPluginEmbeddedAgent`.
- Completing the requested seam therefore requires coordinated core changes to pass the bound plugin runtime into the Telegram bot lifecycle, add a public result-only wrapper, and then add the adapter. I did not fabricate that runtime scope or import internal runner code. Production code remains unchanged; WORKLOG is the only touched artifact in this lane.

## 2026-09-06 inline lifecycle wiring checkpoint

- The monitor contract intentionally exposes `opts.channelRuntime` as `PluginRuntime["channel"]`, and Telegram only forwards that channel surface into webhook/polling startup. The agent runtime (`PluginRuntime.agent.runEmbeddedAgent`) is not present at this owner boundary. Passing a casted channel object or reading a global runtime store would bypass the bound plugin request scope used by `runPluginEmbeddedAgent` admission.
- Safe next change must be core-owned lifecycle wiring that carries the full bound `PluginRuntime` (or a dedicated, typed external-turn capability) into Telegram account startup. Until that exists, no inline adapter or result-only wrapper can be exercised without violating ownership. No production code changed; no BotFather action performed.

## 2026-09-06 typed external-turn wiring checkpoint

- Added the narrow public capability types in `src/channels/plugins/channel-runtime-surface.types.ts` and re-exported them from `src/plugin-sdk/channel-contract.ts`: `ChannelExternalTurnRequest`, typed `ChannelExternalTurnResult`, and `ChannelExternalTurnRunner`. The capability is optional and result-only; it exposes no runner internals or normal channel delivery.
- Wired the optional capability through Telegram’s existing lifecycle: `monitor.types.ts` → `monitor.ts` webhook opts → `webhook.ts` bot opts → `bot.types.ts` → `bot-core.ts` → `bot-handlers.types.ts`. Added the corresponding optional field to `PluginRuntimeChannel`.
- This is type/lifecycle plumbing only. The core owner still must populate `externalTurns.runResultOnly`; Telegram adapter and exactly-once `answerInlineQuery` remain unimplemented. No BotFather action occurred.
- `git diff --check` passed. Focused Vitest invocations were attempted but did not complete within the bounded run; full TypeScript check hit the repository’s Node heap limit before diagnostics. No claim of compile/test success is made.

## 2026-09-06 inline plumbing reverted

- The optional external-turn types and Telegram forwarding edits were reverted after owner review because no core runtime populated the capability; leaving them would have created an unused production API. Existing dirty files (`PortGuardian.swift`, its tests, `pnpm-lock.yaml`, pre-existing `src/plugins/runtime/types-core.ts`, and the untracked token script) were preserved.
- Final inline status remains: Bot API capability is present, but model-backed inline handling needs a complete core-owned runtime capability and adapter implementation before `/setinline` can be useful. No BotFather action was performed.

## 2026-09-06 consolidated implementation plan

- Detailed three-lane Russian plan is in [`INLINE_COMPANION_WINDOWS_PLAN.md`](INLINE_COMPANION_WINDOWS_PLAN.md): Telegram inline core seam/adapter, Companion pairing recovery, and Windows node/browser self-recovery with acceptance and rollback criteria.
- Direct Telegram evidence additionally confirms a reachable Windows node with a command sent through the wrong shell; Hicks stopped at a recommendation instead of owner-level shell detection, one bounded retry, and visible terminal proof. The plan requires structured Windows-vs-WSL shell targets, absolute WSL path, classified retry, and no completion claim from reachability alone.

## 2026-09-06 authoritative final state audit

- Companion device registry was read through the authoritative public Gateway without mutation: `pending: []`; approved macOS UI records (`openclaw-macos` / `MacBook Pro`) exist but report `connected: false`. No approval is pending or was attempted. The local STRICT-table repair is therefore complete, while authenticated Companion control remains unproven because the app still showed reconnecting after relaunch.
- Windows authoritative node status is PASS: the paired/approved Windows Headless identity is connected; routed `system.which` returned the Windows portable Node; the existing iCloud browser status/tabs/snapshot returned an authenticated Photos page. No photos, login, 2FA, token, or Gateway setting was changed during this audit.
- Windows self-recovery is only partially proven: the owner script recovered a locally induced process stop, but the fresh owner log also contains duplicate termination/owner failure and repeated launches at 02:48, 02:49, and 02:53. Task Scheduler reported `0x800710e0`, no missed runs, and the current process remained alive from 02:53:51 during the probe. Sparse logs cannot support the user's reported 30% uptime as a measured percentage. Required acceptance remains 30 minutes continuous authoritative connectivity plus induced stop→automatic relaunch→registry/routed exec/browser proof, repeated once after Gateway/process restart.
- Final matrix before this source patch: Windows current route and browser proof PASS; Windows end-to-end induced recovery UNKNOWN; Companion local schema repair PASS, pending approval correctly absent, authenticated control UNKNOWN/FAIL in current UI; Telegram inline model-backed handling NOT IMPLEMENTED. After the source patch, Telegram inline is IMPLEMENTED in source with live BotFather/model-backed proof still UNKNOWN. The mandatory Luna final verification gate remains in `INLINE_COMPANION_WINDOWS_PLAN.md`; no lane is complete while an UNKNOWN or blocker remains.

## 2026-09-06 Telegram inline implementation

- Implemented the core-owned result-only capability through the existing plugin runtime owner. `runPluginEmbeddedAgentForResult` uses the canonical embedded runner, suppresses normal message delivery, filters commentary payloads, bounds execution, and returns a typed completed/empty/timeout/error result.
- Bound the capability through `createPluginRuntime` → `createRuntimeChannel` and re-bound it per plugin lifecycle owner in `src/plugins/registry-runtime.ts`; Telegram receives only the narrow `channelRuntime.externalTurns.runResultOnly` capability.
- Added Telegram inline query registration and adapter. It uses configured numeric DM allowlist authorization, stable `telegram:inline:<account>:<sender>` session identity, bounded execution, process-local query-id dedupe, and exactly one `answerInlineQuery` call per accepted query. No BotFather action or new config/SQLite surface was added.
- Focused proof passed: 4 files, 31 tests (`runtime-embedded-agent.runtime`, `runtime-channel`, `bot-handlers.runtime`, `bot-handlers.inline`). `oxlint` passed for all changed source/test files. Full TypeScript check was attempted with `tsgo` and stopped after exceeding the bounded time window without diagnostics; exact-head build and live Telegram inline proof remain mandatory before completion.

## 2026-09-06 lead-reported recovery update (pending Luna independent gate)

- Lead reports the Windows supervisor now matches the owned portable-node distribution command rather than the old loopback route. After task restart the existing PID was preserved without a duplicate; an induced kill was followed by a new PID and launch log in about 12 seconds, with the task running and result `0x41301`. This remains lead evidence until the mandatory independent Luna verification gate is run.
- Lead reports Companion now uses the existing SSH transport after direct WSS TLS hostname mismatch; the native Application Support database was backed up and both empty identity/PortGuardian tables were atomically rebuilt as STRICT with integrity checks. The stale companion tunnel LaunchAgent was disabled, proxy/VLESS agents were untouched, and after relaunch the UI showed connected to the remote Gateway. Independent Luna verification remains required.

## 2026-09-06 Luna inline source gate and live deployment

- Exact-head source gates after the latest latest-query-wins repair: focused Vitest passed 4 files / 38 tests; `pnpm exec oxlint` and `git diff --check` passed; `pnpm tsgo:core`, `pnpm tsgo:extensions`, and `pnpm build` passed. The inline suite covers success, query-id dedupe, empty/timeout/error results, unauthorized and absent capability, answer failure, rapid-query cancellation, rejected superseded answer, and ownership-safe lane cleanup.
- Before mutation, the loaded `/usr/lib/node_modules/openclaw/dist` and package metadata were backed up at `/home/clawd/.openclaw/backups/inline-deploy-20260906-1255`. The built dist was staged through the Windows host, installed as an atomic dist replacement, and the previous dist was retained at `/usr/lib/node_modules/openclaw/dist.pre-inline-20260906`. No token, config, proxy/VLESS route, SQLite state, or BotFather setting was changed.
- Restarted only the owner system unit `openclaw-gateway.service` using the existing sudo owner path. Gateway reached its internal `ready` log; Telegram polling reached `ready`, `connected`, and `probe.ok=true`, with `supportsInlineQueries=true`. Startup logged a pre-existing optional Matrix doctor-contract dependency warning (`matrix-js-sdk/lib/matrix.js`) and orphan-run pruning; neither prevented Gateway readiness or Telegram polling. The unit remained `activating (start-post)` in the readiness wrapper during the bounded observation, so service-manager ACTIVE proof is not yet PASS.
- A later bounded status check observed `openclaw-gateway.service` `active`; the earlier `activating (start-post)` interval is retained as restart behavior evidence, and no failure is claimed from it.
- Authoritative public Gateway read-only proof after restart reports both approved Mac and Windows node identities connected. Windows `nodes invoke system.which` reached the node successfully, but returned an empty `bins` payload for the requested names; this proves routed invocation, not a shell marker. Browser proxy calls with `/json/version` and `/json/list` returned 404, so fresh iCloud status/tabs/snapshot proof is UNKNOWN in this observation and no browser mutation occurred.
- Live inline query and normal-DM user-visible proof remain UNKNOWN: Bot API capability and polling health are proven, but no Telegram user client/e2e credential was available in this lane to issue the exact inline query and verify one visible article plus a normal DM response. BotFather `/setinline` was not sent. The prior observed query failure predates this deployment and is not counted as a post-deploy result.
- A post-deploy normal-message test initially exposed a package-owner gap: the new dist resolved the Codex harness but the installed package lacked its canonical Linux managed artifact, producing `Managed Codex app-server binary was not found` and a generic error reply. The existing project-owned packages at `/home/clawd/.openclaw/npm/projects/openclaw-codex-8902d781d4/node_modules/@openai/codex-linux-x64` and `@openai/codex` were copied into the installed package's existing `node_modules/@openai` owner path, then the same Gateway unit was restarted. Subsequent logs show successful agent runs; no new config or token was introduced.
- Corrected browser read-only proof uses the existing CLI profile selector: `openclaw browser status/tabs --browser-profile icloud` through the authoritative public Gateway. It reports `running=true`, `cdpReady=true`, `pageReady=true`, attach-only port `18801`, and one existing `Фото iCloud` tab. No browser mutation occurred.
- Rollback is to stop the same Gateway owner, restore `/usr/lib/node_modules/openclaw/dist.pre-inline-20260906` (or the preserved backup), and start the same unit. Final Luna gate remains open until service ACTIVE, live inline+DM proof, fresh Windows browser proof, and Companion authenticated control are recorded.

## 2026-09-06 independent Luna Windows/Companion acceptance spot-check

- Windows Task Scheduler read-only inspection confirms `OpenClawNodeStable` still owns `openclaw-node-service-20260828.ps1`; the task reports `LastTaskResult=267009 (0x41301)` and no future scheduled run. No task or process mutation was made. This is owner evidence, not an induced recovery proof.
- Authoritative browser CLI proof was rerun with the configured profile selector: `browser status/tabs --browser-profile icloud` reports `running=true`, `cdpReady=true`, `pageReady=true`, attach-only CDP 18801, and `Фото iCloud` at `https://www.icloud.com/photos/`. No photo mutation occurred.
- Companion UI was independently inspected with CUA: OpenClaw.app is running, the main page is loaded without a reconnecting banner, sessions are visible, the `Telegram / inline` group is present, and the `Рабочая доска` Workboard link is visible. This confirms the local UI/control surface and Workboard visibility; it is not a separate authenticated remote control-ping proof.
- Additional owner checks: `OpenClawNodeStable` is Running with boot trigger, 30-second delay, `RestartCount=999`, `RestartInterval=1m`, and `MultipleInstances=IgnoreNew`; CIM reports one owned portable `node.exe` process (PID omitted here) through `cmd.exe`/`node.cmd`, with no duplicate node. Historical duplicate/error entries remain in `node-service.log`, so induced kill/reconnect proof is still separate. Local Companion SQLite integrity is `ok`, both repaired tables are `STRICT`, the legacy companion tunnel LaunchAgent is disabled, and the app-owned/proxy LaunchAgents are alive.
- Controlled recovery proof completed read-only around the owner mutation: the single owned node process was intentionally stopped, Task Scheduler relaunched a new PID within the bounded observation, authoritative public Gateway then reported Windows Headless `connected=true` with `lastSeenReason=connect`, and `browser status/tabs --browser-profile icloud` again reported `running/cdpReady/pageReady=true` with the existing `Фото iCloud` tab. No duplicate process appeared. This proves one stop→relaunch→registry/browser recovery cycle; the 30-minute soak and post-Gateway-restart repeat remain open.

## 2026-09-07 Windows/Companion final acceptance extension

- The bounded soak collected 30 probes over 35 minutes (`/tmp/hicks-soak-simple.jsonl`): 24 direct successful samples plus four isolated `node=false` snapshots and one SSH banner timeout. Gateway stayed active in all parsed samples; browser remained ready in every non-SSH sample. Immediate authoritative re-probes after each false snapshot reported Windows Headless connected, so no sustained node/browser outage was proven; the remaining evidence is a transient status/transport observation race with a maximum unconfirmed interval of one probe period (~70s).
- After the soak, the owner system `openclaw-gateway.service` was restarted. It reached `active`; authoritative public registry again reported Windows Headless connected, and the iCloud profile reported `running=true`, `cdpReady=true`, `pageReady=true`, attach-only CDP `18801`, and the existing Photos tab.
- A second controlled Windows process stop after Gateway restart terminated the sole owned node process; Task Scheduler relaunched a new PID within ~10 seconds, with no duplicate owner. After an additional bounded wait, authoritative Gateway again reported Windows Headless connected. Local WSL Gateway remained reachable and Companion's actual SSH-tunnel owner registry showed `pending=0` plus an approved `openclaw-macos` UI record connected; public TLS registry's disconnected Mac UI records belong to a different Gateway owner and are not valid Companion acceptance evidence.
- Companion UI/control check remains: OpenClaw.app shows sessions and Workboard surface without a reconnecting banner; local database integrity/STRICT checks and owner/proxy agent checks remain green. No new pairing approval or SQLite mutation was made.

## 2026-09-06 Luna independent source recheck

- Rechecked the current working tree after the live command-path repair. `git diff --check` passed and the result-only runtime now classifies an un-aborted runner exception as `{ kind: "error", code: "failed" }`; only an actual request/controller abort maps to `aborted`. Error payloads are rejected before text aggregation, so provider failures cannot become selectable inline articles.
- Focused Vitest with single-thread execution passed `src/plugins/runtime/runtime-embedded-agent.runtime.test.ts`: 19 tests, including the new runner-exception and `isError` payload regression. Telegram inline live result and normal-DM user-visible outcome remain UNKNOWN until the operator smoke test is observed.

## 2026-09-06 Telegram inline authorization repair

- Independent review found the inline adapter had compared the unsupported literal `dmPolicy: "allow"`; Telegram's canonical policies are `open`, `allowlist`, `pairing`, and `disabled`, with `pairing` as the default. The live account omitted `dmPolicy`, so every inline query was denied before reaching Codex.
- Repaired `extensions/telegram/src/bot-handlers.inline.ts` to use the canonical Telegram ingress resolver, effective DM policy, configured allowlist, and the Gateway-owned pairing allow store. No bypass config or new surface was added.
- Added inline regressions for default pairing with a stored approved sender, allowlist allow/deny behavior, and preserved absent-capability/unauthorized coverage. Focused inline suite passed 12 tests; oxlint and diff-check passed. Live deployment/restart and user-visible smoke proof remain pending this source change.

## 2026-09-06 live policy-fix deployment (lead)

- Deployed the post-review Telegram inline authorization repair atomically from the current built `dist`; retained the previous live dist as `/usr/lib/node_modules/openclaw/dist.pre-policy-<deployment timestamp>` on Hicks.
- Restarted only `openclaw-gateway.service`; fresh checks returned Gateway `ready: true`, systemd `active`, configured Codex app-server command `/home/clawd/.openclaw/npm/projects/openclaw-codex-8902d781d4/node_modules/.bin/codex`, and zero fresh `Managed Codex app-server binary was not found` records.
- Focused source proof remains 31/31 tests. Telegram user-visible inline and normal-DM proof is still intentionally UNKNOWN until the operator sends a post-deploy smoke test; no prior pre-fix update is counted.

## 2026-09-06 inline fast-path repair (Luna)

- Fresh live logs for updates `182010584` and `182010585` showed both inline queries were received at `18:02:32`, entered the shared spool with `queued=2`, and only attempted `answerInlineQuery` at `18:02:39` and `18:02:48`; Telegram returned `400 query is too old`. The failure was an ingress ownership violation, not model authorization.
- Repaired the isolated polling worker to classify `inline_query` updates separately. The parent polling session advances the Telegram offset and dispatches the bot handler immediately without durable spool/retry adoption; normal message updates retain the existing durable-before-offset path. Late Telegram rejection is logged and never replayed with a stale query id.
- Added a worker regression proving inline updates emit the dedicated fast-path message and never enter the durable update path. Focused polling/worker suite passed 2 files / 111 tests; `oxlint` and `git diff --check` passed. Build/deploy/live verification remains required; post-deploy Telegram user-visible proof is UNKNOWN until a fresh operator smoke test.
- Rebuilt exact current source (`pnpm tsgo:extensions` and `pnpm build` passed; build completed with only existing third-party eval/browser warnings), streamed the dist to Hicks, backed up live dist/config at `/home/clawd/.openclaw/backups/inline-fast-20260906-1815`, atomically replaced `/usr/lib/node_modules/openclaw/dist`, and restarted only `openclaw-gateway.service`.
- Fresh live evidence: systemd `active`, Gateway `ready` at `18:19:32`, Telegram isolated polling started at `18:19:34`, polling offset resumed at `182010586`, configured Codex command remains `/home/clawd/.openclaw/npm/projects/openclaw-codex-8902d781d4/node_modules/.bin/codex`. No post-deploy missing app-server error observed. Windows Headless and current Mac node are connected in authoritative `nodes status`; Companion authenticated control and Telegram user-visible inline/DM response remain UNKNOWN pending operator smoke.

## 2026-09-06 inline latency export audit (Luna)

- Read-only analysis of `/Users/wholewnewpea/Downloads/Telegram Desktop/ChatExport_2026-09-06 (2)/messages.html` found no successful Hicks inline result. It contains an `@hickss_bot` test at `19:04:44` with no corresponding bot answer in the export; the nearby `@alisa` messages are explanatory prose and explicitly say that Alisa has no Telegram inline mode, so they are not stability evidence.
- Correlated Hicks live evidence for updates `182010584/585`: worker received update `182010584` at `18:02:32.091`, spooled it at `18:02:32.094`, and the failed `answerInlineQuery` was attempted at `18:02:39.875` (7.781 s after worker receipt). This proves spool/retry added the wrong owner boundary; it does not prove the query was already old before worker receipt or that a two-phase UX is required.
- Per the new operator direction, the two-phase placeholder WIP was not deployed. The source is restored to the one-phase model-backed fast-path, while the worker still routes inline updates outside durable spool. Further work should capture Telegram update ingress timestamp, worker handoff, model start/end, API request start/end, offset/poll batch order, duplicate/retry count, and network timing before changing UX.
- Added exact one-phase timing logs keyed by query id: `inline_received`, `inline_authorized`, `model_start`, `model_end`, `answer_start`, and `answer_end`; query prompts remain excluded. The handler bounds the model prompt to Telegram's documented 256-character inline query limit and retains `cache_time=0`, `is_personal=true`, and the fixed 15-byte result id.
- Rebuilt exact head (`pnpm tsgo:extensions`, full `pnpm build`) and atomically deployed to Hicks backup `/home/clawd/.openclaw/backups/inline-timing-20260906-1930`; restarted only `openclaw-gateway.service`. Gateway reached `ready` at `19:42:12`, Telegram provider/polling started at `19:42:14/16`. No user post-deploy result yet; Telegram PASS remains UNKNOWN.

## 2026-09-06 Telegram sequential lane repair (#54794 / #51963)

- Confirmed the remaining blocker: `getTelegramSequentialConstraints()` previously returned the global `telegram:unknown` lane for `inline_query`, so all inline handlers waited behind the prior model turn. The repair returns the grammY sequentialize contract's empty constraint array for inline queries only; ordinary message/thread lanes are unchanged.
- Added a behavioral concurrency regression using the same sequentializer contract: two inline updates enter concurrently while the first is held; ordinary lane serialization tests remain intact. Focused suite passed 3 files / 82 tests, sequential-key suite passed 65 tests; oxlint and diff-check passed.
- Added process-local WeakMap ingress timing carried from polling fast-path and webhook drain into the handler, so `inline_received` measures from the actual ingress boundary without persistent state.
- Rebuilt exact head (`pnpm tsgo:extensions`, full `pnpm build`) and atomically deployed current dist with backup `/home/clawd/.openclaw/backups/inline-seq-20260906-2005`; restarted only `openclaw-gateway.service`. Fresh Gateway ready: `20:07:28`; Telegram provider/polling started: `20:07:29/44`. No new missing app-server error observed. Telegram user-visible PASS remains UNKNOWN until a fresh test; phase logs are ready for correlation.
- Fresh user test correlated: update `182010590` / query `1509074302598617169` was received at `20:16:23.412` and superseded before authorization/model phases; update `182010591` / query `1509074304781093406` was received at `20:16:23.860`, authorized at +14 ms, model started at +166 ms, model completed at +26,223 ms, and `answerInlineQuery` began at +26,224 ms. Telegram returned `400 query is too old and response timeout expired or query ID is invalid` at `20:16:50.252`.
- The sequential lane repair worked: both updates entered the inline fast-path within 448 ms, no `telegram:unknown` wait or durable spool/retry appeared, and no duplicate replay was observed. The remaining live blocker is model latency (26.2 s) exceeding Telegram's undocumented `BOT_RESPONSE_TIMEOUT`; no article was successfully returned and no `answer_end` exists. This is a confirmed one-phase product deadline failure, not a remaining global-lane defect.

## 2026-09-06 Telegram two-phase inline protocol repair (Luna)

- Replaced the one-phase model-before-answer path after live query `1509074304781093406` took 26,223 ms and Telegram rejected the late `answerInlineQuery`. Inline queries now authenticate, immediately return exactly one personal article with `cache_time=0`, a bounded prompt/result id, and an inline keyboard; model work starts only after `chosen_inline_result`.
- Correlation is sender plus a query-derived result id (bounded to Telegram's 64-byte result-id contract), held in a capped 10-minute process-local map. Chosen handling revalidates authorization, runs result-only, and edits the exact `inline_message_id`; failures become visible text. A per-sender selected-run map aborts an older active turn when a newer query arrives without invalidating independently selectable placeholders.
- `inline_query` and `chosen_inline_result` both bypass the global `telegram:unknown` grammY lane. Polling and webhook fast paths retain ingress timing and durable normal-message behavior. Added regression coverage for immediate placeholder/no model, exact chosen correlation/edit, wrong sender/missing inline message/duplicate/expiry/error, unique ids, and chosen sequential constraints.
- Focused Telegram suite passed 3 files / 85 tests; oxlint, `git diff --check`, and `pnpm tsgo:extensions` passed. Full build is in progress; deployment and fresh user-visible chosen/edit proof remain pending. BotFather inline feedback is an operator prerequisite for `chosen_inline_result`; until that update and subsequent edit are observed, Telegram PASS is UNKNOWN.
- Live model evidence for the failed one-phase query: `openai/gpt-5.6-luna`, `thinking=minimal`, `fast=off`, OpenAI ChatGPT Responses transport. `ChannelExternalTurnRequest` has no model/thinking override, so no inline-only acceleration was introduced and global model/config were unchanged.
- Full build completed (`pnpm build`, 4m19.6s; only existing third-party eval/browser warnings). Dist was streamed and atomically deployed to Hicks; previous live dist/config backed up at `/home/clawd/.openclaw/backups/inline-two-phase-20260906-2049`; only `openclaw-gateway.service` restarted. Independent post-deploy checks: systemd `active`, fresh `gateway ready` at `20:55:13` local, live dist contains `chosen_inline_result` worker path, and no new missing-app-server record after restart. Chosen/edit Telegram proof is still UNKNOWN until the operator selects a placeholder and fresh logs show `chosen_inline_result` plus `editMessageText`.

## 2026-09-06 post-deploy inline/SSH/orchestration audit (Luna)

- Fresh live inline evidence at `22:21:08`: query `1509074305998394097` received a placeholder successfully (`resultId=hicks-1509074305998394097`, `answerMs=860`, `ingressAgeMs=92`). There is no subsequent `chosen_inline_result`, model start for this inline query, or `editMessageText` record in the current Gateway log. This proves the two-phase first leg works and the second leg is absent; Telegram PASS remains UNKNOWN. The missing event is consistent with BotFather Inline Feedback not being enabled. Bot API direct polling check timed out through the current route, so no claim about remote API state is made.
- Independently verified the live dist includes `chosen_inline_result` in both `telegram-ingress-worker.runtime.js` and `telegram-ingress-drain-factory-CfODLY2A.js`; worker advanced polling offset normally. No code-side omission of the handler was found.
- Hicks SSH session evidence at `22:25`: it executed `ssh -G clawd@192.168.0.181`, a BatchMode connectivity probe, and a direct `ssh` command through `node.invoke`; logs show the SSH client and target route were available. The failure was policy refusal to use an owner-supplied private key, not missing SSH or unreachable host. The supplied key was not printed, stored in repo/config, or reused by this audit.
- Updated the actually loaded `/home/clawd/.openclaw/workspace/SOUL.md` and `/home/clawd/.openclaw/workspace/TOOLS.md` with owner-authorized secure credential use, self-execution/provisioning, proxy preservation, observable proof, and parallel native OpenClaw worker orchestration. Prompt mutation is file-backed and will apply to newly assembled sessions; no secret was written. A nonsecret fixture was not used to access the target host, so SSH task completion remains UNKNOWN.

## 2026-09-06 Hicks owner policy source/live correction (Luna)

- Root cause of the previous ineffective policy: the configured `main` agent workspace is `/home/clawd/.openclaw/obsidian-operating-base` (from live `openclaw.json`), while the earlier mutation targeted `/home/clawd/.openclaw/workspace`. The latter is not the loaded main workspace. The canonical source for prompt behavior is now versioned in `src/agents/system-prompt.ts` (`buildOwnerExecutionPolicySection`), with focused assembly coverage in `src/agents/system-prompt.test.ts`; the existing `obsidian-operating-base` customization asset carries the live Front/Orchestrator and owner policy. No inline runtime code was changed.
- Backed up the actual live files before mutation at `/home/clawd/.openclaw/backups/hicks-policy-20260906-2245/{AGENTS.md,SOUL.md}` and appended the role boundary, parallel worker, fast-memory, owner-secret, internal-machine, proxy/VLESS, and evidence rules to `/home/clawd/.openclaw/obsidian-operating-base/{AGENTS.md,SOUL.md}`. Rollback is a direct copy of those two backup files over the live counterparts; no secret was written or echoed. The stale `/home/clawd/.openclaw/workspace` edits were left untouched because they are pre-existing shared work.
- Fresh live `openclaw agent --agent main --session-key owner-policy-fixture-win-20260906 --json` reported `workspaceDir=/home/clawd/.openclaw/obsidian-operating-base`, injected `AGENTS.md` raw/injected `13362` chars and `SOUL.md` `5179` chars, both untruncated, and native `sessions_spawn`/`node_exec` tools. The nonsecret fixture created and reread `AUTONOMY_FIXTURE_OK` on the Windows node, proving self-execution instead of advice; the temporary file was removed afterward. A Linux-path variant correctly exposed a runtime boundary: `node_exec` is routed to the Windows node, so it did not claim a false local WSL success.
- Focused source test passed: `pnpm vitest run src/agents/system-prompt.test.ts -t 'assembles owner execution policy' --no-file-parallelism --maxWorkers=1` (`2 passed`, `262 skipped`). The live JSON system-prompt hash was `3ec2db024ecff84339356ec25dd7ad72308eb82dc46805bfc795e9cf02a89454`; it proves the workspace customization is loaded now. The new hardcoded source policy is versioned but requires the normal dist build/deploy owner to become live; prompt-only policy cannot create unavailable runtime routing.
- Original AP/SSH task remains UNKNOWN: no key was printed, stored, or used by this policy verification, and no AP mutation was performed without a precise live command and boundary proof.
- Added the normative Russian target contract and migration/acceptance matrix in `HICKS_ARCHITECTURE_PLAN.md`; requested immutable-objective memory note is `/Users/wholewnewpea/.codex/memories/extensions/ad_hoc/notes/20260906-225709-hicks-target-architecture.md`. The plan distinguishes current evidence from planned behavior and requires zero `UNKNOWN` before end-to-end acceptance.
- Expanded the plan for the owner's whole-Windows boundary: WSL Linux, persistent privileged native Windows Node, and persistent user-session browser Node are separate execution planes selected by structured Gateway calls. Added no-interactive-login, no ad hoc WSL-to-Windows SSH, auto-start/watchdog/reconnect, and recovery acceptance for node kill, WSL/Gateway restart, network interruption and Windows reboot, including native exec and browser evidence.

## 2026-09-06 source-of-truth customization contract (Luna)

- Added versioned `customization/hicks/OWNER_POLICY.md` and mapped it in `HICKS_ARCHITECTURE_PLAN.md`. It records Front/Orchestrator roles, native Luna worker fan-out, Workboard ownership, owner-authorized internal execution and secret handling, Windows/WSL/browser planes, proxy/VLESS preservation, inline confirmation/final DM delivery, rollback, and evidence rules.
- Source prompt remains `src/agents/system-prompt.ts` with tests in `src/agents/system-prompt.test.ts`; live main workspace is `/home/clawd/.openclaw/obsidian-operating-base`, not the stale `/home/clawd/.openclaw/workspace`. Fresh runtime/Companion/Telegram worker-overlap acceptance is still UNKNOWN; no completion claim is made from prompt or installed-dist presence.

## 2026-09-06 Workboard orchestration owner binding (Luna)

- Audited the canonical Workboard plugin before adding behavior: `extensions/workboard/src/tools-orchestration.ts`, `dispatcher.ts`, `gateway-workspace-methods.ts`, `lifecycle-sync.ts`, and `docs/cli/workboard.md` already provide persistent cards, dependency/decompose, claims, dispatch, worker logs, recovery, and Companion projection. No new SQLite schema was introduced.
- Captured the current runtime gap: the existing owner policy described worker fan-out, but the generated prompt did not explicitly bind available Workboard tools as the authoritative ledger. Added source-owned capability wiring in `src/agents/system-prompt.ts`: when `workboard_create`/`workboard_decompose` are callable, Hicks must create/update parent and lane cards, record dependencies/claims/progress/retries/terminal evidence, and read board state for status. Added regression in `src/agents/system-prompt.test.ts`.
- Focused prompt suite passed 266 tests; oxlint and diff-check passed. This is a coherent source owner-boundary slice, not end-to-end routing acceptance: live dist deployment, three overlapping Luna workers, Companion board proof, status-while-running, restart recovery and final exactly-once Telegram evidence remain UNKNOWN.
- Deployment owner must package the full install (including managed Codex/app-server dependencies) atomically; the previous dist-only deployment omitted the managed binary and caused generic DM/inline failures. Preflight exact binary resolution before replacing live artifacts; retain rollback.

## 2026-09-06 managed Codex deployment drift audit (Luna)

- Confirmed the live failure class: the earlier atomic replacement changed `/usr/lib/node_modules/openclaw/dist` while leaving the package/dependency boundary implicit. The first post-deploy normal-message smoke then produced `Managed Codex app-server binary was not found`; this is a deployment-bundle defect, not a provider or proxy/VLESS failure.
- Current live repair is verified read-only through the Windows host's WSL boundary. Installed `/usr/lib/node_modules/openclaw/dist/extensions/codex/package.json` declares `@openai/codex` `0.151.0` and all platform aliases; `/usr/lib/node_modules/openclaw/node_modules/@openai/codex*` now matches the existing owner-managed project at `/home/clawd/.openclaw/npm/projects/openclaw-codex-8902d781d4/node_modules/@openai/codex*`. The native Linux executable reports `codex-cli 0.151.0`.
- Reproducible preflight passed: `openclaw doctor --lint --only codex/managed-app-server --json` returned `{"ok":true,"checksRun":1,"checksSkipped":1,"findings":[]}`. This proves package metadata, native artifact and executable/version resolution at the current owner path. It does not by itself prove a Telegram-visible result.
- The post-repair Gateway smoke also passed on the authoritative WSL Gateway: a bounded `openclaw agent --agent main --session-key drift-acceptance-20260906-gw3 --message "Reply with exact ASCII text DRIFT_SMOKE_OK only." --thinking off --timeout 60 --json` returned `status=ok` and the exact marker `DRIFT_SMOKE_OK`. The first `--local` attempt was correctly refused because the live Gateway already owned the state directory; no Gateway stop/restart was performed for the smoke.
- Drift mapping is now explicit: source plugin manifest `extensions/codex/package.json` -> built `dist/extensions/codex/package.json` -> installed `/usr/lib/node_modules/openclaw/dist` plus `/usr/lib/node_modules/openclaw/node_modules/@openai/codex*` -> runtime resolved command -> Gateway agent smoke. A dist-only copy breaks the second edge and must fail preflight before restart.
- No config, token, SQLite state, browser state, proxy/VLESS route, Windows task or node identity was changed in this audit. Existing live package copies and prior deployment backups remain the rollback source; rollback must restore the complete dist plus managed dependency tree, then rerun the doctor check and a bounded nonsecret agent smoke before declaring `ready`.
- Remaining gap: the current deployment owner is still procedural rather than a checked-in full-bundle transaction. Future rollout must stage a manifest-bound bundle, validate the exact executable before cutover, retain a full-runtime rollback copy, restart only the owning Gateway, and require the post-restart agent smoke. The current Gateway is usable after manual dependency restoration, but this drift class remains a release blocker until that owner path is automated.

## 2026-09-06 Telegram Front admission slice (Luna)

- Captured the pre-fix owner boundary: `extensions/telegram/src/bot-handlers.inbound-pipeline.ts` awaited `processInboundMessage` inside `handleMessage`, so a long model/tool turn occupied Telegram admission and could delay the next status/casual update on the same lane.
- Added deterministic Front admission classification. Direct actionable requests (bounded Russian/English action vocabulary or long task text) now receive an immediate visible acknowledgement and schedule `handleInboundMessageLike` in the background; casual and slash/control messages retain the synchronous Front path. Background errors are recorded through the existing runtime logger and the existing processing pipeline remains the final delivery owner, avoiding a new store/schema.
- Added regression coverage for casual/control vs actionable routing and updated handler registration coverage for `chosen_inline_result`. Focused Telegram tests passed 3/3; oxlint, `pnpm tsgo:extensions`, and diff-check passed.
- This is a production runtime vertical slice, but not complete orchestration acceptance: it does not yet create/decompose Workboard cards from the Telegram handler itself or prove three overlapping Luna workers, status while active, exactly-once final delivery, or restart recovery. Full package deployment requires managed Codex binary preflight; no live deployment or PASS claim made here.

## 2026-09-06 correction: inbound Front classifier reverted

- Reverted the experimental Telegram string/length classifier and detached `void` dispatch from `bot-handlers.inbound-pipeline.ts`. It violated generic intent ownership, had silent acknowledgement failure, and lacked durable admission, exactly-once delivery, and restart recovery. Restored the original debounce-key regression suite in `bot-handlers.inbound-pipeline.test.ts`.
- No deployment occurred. The required next implementation owner is generic agent/task admission using existing Workboard/TaskFlow/session APIs; Telegram must not classify work by hardcoded language keywords.

## 2026-09-06 native Luna worker routing configuration

- Audited live `sessions_spawn` contract and existing limits. Main had `delegationMode=prefer`, `maxSpawnDepth=1`, `maxChildrenPerAgent=4`, `maxConcurrent=4`, but `agents.defaults.subagents.model` was unset and `main` was absent from the explicit allowlist; worker model therefore inherited inconsistently and the intended dedicated Hicks orchestrator target could be rejected.
- Applied the owner-authorized existing config surface atomically with backup `/home/clawd/.openclaw/backups/hicks-orchestration-20260906-2318`: set `agents.defaults.subagents.model` to `openai/gpt-5.6-luna` and added `main` to `agents.defaults.subagents.allowAgents`. Gateway hot-reloaded exactly these two keys at `23:22:30/31`; no restart, proxy, secret, or SQLite change.
- Effective cap remains four concurrent children. This establishes actual native Luna worker selection/target admission, but does not by itself create a Front disposition or Workboard parent; overlap, Companion cards, status lane, final delivery, and recovery remain UNKNOWN.

## 2026-09-06 checked-in full-bundle deployment owner (Luna)

- Closed the procedural deployment gap with `scripts/hicks-deploy-runtime.mjs`. The owner copies the existing install as a complete stage, overlays exact source `dist`, `openclaw.mjs`, root package metadata, and both managed Codex packages (`@openai/codex` plus the current-platform optional artifact), and writes `.openclaw-deploy-manifest.json`. It never copies `OPENCLAW_STATE_DIR`, config, SQLite state, browser data, or proxy/VLESS material.
- Preflight is owner-bound: the existing `assertCodexReleasePackageContract` verifies manifest dependency/version, platform metadata, executable containment and `codex-cli 0.151.0`; the staged runtime then must return the existing doctor JSON contract (`ok=true`, empty findings) before any service stop. The cutover is same-filesystem rename of the old full tree to a timestamped backup followed by the stage rename; service start and bounded `agent --json` smoke with marker `HICKS_DEPLOY_SMOKE_OK` are required.
- Failure after cutover stops the new service, retains the failed tree for diagnosis, restores the complete backup tree, and starts the owner service again. Preflight failure leaves the live install untouched. `--dry-run` performs stage + contract + doctor without cutover.
- Existing `scripts/update-gateway.sh` now delegates to this transaction when `OPENCLAW_UPDATE_INSTALL_ROOT` is set, so the source checkout updater remains the deployment entry point while the package install cutover has one canonical owner. Its legacy source-checkout restart path remains unchanged when that variable is absent.
- Added `test/scripts/hicks-deploy-runtime.test.ts`: dry-run proves no install/state mutation, success proves new bundle plus complete old backup, and failed smoke proves full-tree rollback while preserving external state. Focused run passed 3/3 tests. No live rollout was performed in this turn; the next deployment is authorized only after Runtime Luna's safe slice and must record the real backup/doctor/smoke evidence here.

## 2026-09-06 — native Hicks Front/Orchestrator/Workboard synthetic vertical slice

- **Authoritative live registry:** `main` is the only Telegram-bound agent (`bindings: 1`); `hicks-orchestrator` is a distinct config-created agent with workspace `/home/clawd/.openclaw/workspace-orchestrator`, model `openai/gpt-5.6-luna`, and `bindings: 0`. No Telegram route is attached to the orchestrator.
- **Runtime path exercised:** `hicks-orchestrator` session `agent:hicks-orchestrator:synthetic-orchestrator-20260906` used the native Workboard tools in order (`workboard_create` → `workboard_claim` → `workboard_decompose` → `workboard_dispatch`) and native `sessions_spawn`; the session trajectory records `sessions_spawn` three times and completed with `openai/gpt-5.6-luna` at 20:27:05Z. A follow-up reconciliation turn read the board, commented, and completed the parent at 20:29:35Z.
- **Worker evidence:** three durable subagent tasks under the same orchestrator session were `succeeded`/`delivered`, with native worker identities and Workboard card ids. Their starts overlap: lane 1 started 20:27:52.002Z, lane 2 20:27:53.036Z, lane 3 20:28:09.7515178Z; all were independently tracked and terminal.
- **Limit:** this is a synthetic orchestrator/board proof, not yet Telegram Front user-visible proof. The current source contract makes Front the sole channel role and directs actionable work to the separate orchestrator; no direct Telegram binding was added. End-to-end immediate Front acknowledgement + exactly-once final Telegram delivery remains UNKNOWN until a fresh user-visible test is run.
- **Rollback:** restore the config backup made before orchestrator creation (`/home/clawd/.openclaw/backups/hicks-orchestrator-20260906-2326`) and remove the `hicks-orchestrator` agent/workspace if the topology is rejected; source prompt changes remain versioned in `src/agents/system-prompt.ts`.

## 2026-09-07 Front fast-memory audit (Luna)

- Current source already has a run-scoped prepared memory seam: `src/plugins/memory-state.ts` prepares immutable prompt lines before synchronous assembly, and `src/agents/embedded-agent-runner/run/attempt-system-prompt-prepare.ts` invokes it before building the model prompt. The canonical memory plugin remains `extensions/memory-core`; no second store or SQLite schema is present.
- The only existing bounded automatic bootstrap is repository-scoped `src/agents/project-memory-bootstrap.ts` (2,000 characters, deterministic importance/path/line ordering). It calls `getMemorySearchManager().listCuratedProjectCandidates({limit:48})`, so it is not a local Front fast path and may incur memory-index/provider work. It is also only enabled when `activeProjectKeys` exists; it cannot provide owner profile facts.
- Live main workspace is `/home/clawd/.openclaw/obsidian-operating-base`. `USER.md` is 6,614 bytes and contains the canonical owner name, timezone, and preferences; `MEMORY.md` is 10,598 bytes. No dedicated `FRONT_PROFILE`, `CURRENT_TASK`, or equivalent file was found. Existing bootstrap already injects these context files, so adding a duplicate profile section would increase prompt size and create stale/conflicting facts.
- Read-only latency probes through the owning Gateway answered name and timezone correctly in 8.053s and 7.126s respectively (two fresh runs, `--thinking off`). This measures the full model turn, not an isolated memory lookup; it does not reproduce the reported ~30s or prove that memory search caused it. No source patch is justified yet.
- Current gap is architectural: there is no user-message-aware Front disposition/local answer owner in the core memory path. Prompt policy saying “fast profile” cannot prevent a model turn or worker. A safe implementation must first add/locate the canonical Front admission contract, then inject a bounded snapshot from the already loaded USER/context files without invoking `getMemorySearchManager`; long-term `memory_search` remains the detail path. Current-task summary has no canonical source in the live workspace, so it must not be invented.

## 2026-09-07 Telegram inline commit fallback (Luna)

- Added a narrow optional `TelegramInlineTaskCommit` seam in `extensions/telegram/src/bot.types.ts`. It is an injected owner callback; no Telegram adapter imports core runner internals and no new config/store is added.
- Inline query still answers one bounded personal placeholder and never starts model/task work while the user types. The article now includes an explicit `callback_data` commit button for installations where Telegram inline feedback is unavailable. The callback claims the pending correlation synchronously, authenticates by the existing pending sender binding, and calls the injected owner callback exactly once; rejected/failed commits release the claim for retry, accepted commits enter the existing TTL dedupe set.
- Focused inline suite passed 6/6; extension TypeScript and oxlint passed. Runtime callback population and final authorized-DM delivery remain pending the generic Front/Orchestrator seam; the existing chosen-inline-result path remains unchanged until that owner contract lands. No deployment performed.
- Added `extensions/telegram/src/inline-callback-data.ts` and a generic-router fence in `bot-handlers.callback-router.ts` so the plugin-owned commit callback cannot be reinterpreted as a synthetic model callback or trigger a duplicate turn. This keeps callback ownership at the inline adapter boundary.
- Independent review found and repaired callback-only admission (placeholder no longer depends on `externalTurns`) and released the claim when the injected commit seam is unavailable; added fresh authorization immediately before commit. Focused inline suite is now 7/7. The old `chosen_inline_result` branch still runs result-only and edits the inline message; it must be migrated to the same Front commit/final-DM owner before deployment.

## 2026-09-07 Telegram inline commit owner cleanup (Luna)

- Removed the legacy `chosen_inline_result -> runResultOnly -> editMessageText` path. Both `chosen_inline_result` and the explicit callback now share the injected `externalTurns.commitTask` owner and the same sender/result claim, so confirmed inline tasks enter one canonical Front admission path.
- Distinct confirmed result ids remain independent tasks. Unconfirmed typing creates only a short-lived placeholder; if the commit capability is absent, the handler answers an empty result and stores no actionable pending entry.
- TTL and capacity pruning now aborts each owned pending controller and clears only its matching active lane. Correlation keys include account, sender, and result id to avoid cross-account collisions.
- Focused suite: `extensions/telegram/src/bot-handlers.inline.test.ts` 7/7; oxlint and `git diff --check` pass. `pnpm tsgo:extensions` remains blocked by shared Runtime edits missing `subagent.spawnVisible` implementations in `src/plugin-sdk/test-helpers/plugin-runtime-mock.ts`, `src/plugins/loader-runtime-load.ts`, and `src/plugins/registry-runtime.ts`.
- No deploy or live Telegram action performed. Final DM delivery remains owned by the pending core Front/Workboard completion seam and requires separate end-to-end proof.

## 2026-09-07 proxy-chain remediation (Luna)

- Read-only owner mapping found the Windows chain: `192.168.0.20:40222 -> WSL 172.20.1.122:22` and `172.20.0.1:40223 -> 127.0.0.1:40222` are Windows portproxy entries. Port 40222 is owned by the Windows OpenSSH service process; 40223 is owned by the Windows IP Helper service. No WSL listener exists on 40222/40223. The configured OpenClaw HTTP proxy is a separate explicit route to the redacted VPS endpoint on port 17890.
- Root cause proven for the local VLESS process outage: WSL `sing-box.service` had 10,505+ restart attempts and failed every 10 seconds because `/etc/sing-box/config.json` bound `proxy-in` to absent interface address `172.17.0.1:1080`; current WSL address is `172.20.1.122`. `singbox-transparent-proxy.service` was independently failed because the WSL nftables kernel lacks the `redirect` rule capability (`Could not process rule: No such file or directory`).
- Applied reversible owner repair with backup `/etc/sing-box/config.json.bak-luna-20260907-014254`: changed only `proxy-in.listen` from the absent address to `127.0.0.1`. `sing-box check` passed; `sing-box.service` is now active and listening on `127.0.0.1:1080` and `127.0.0.1:15080`. The transparent nftables service remains failed and was not weakened or bypassed.
- Explicit proxy probes after repair: 20/20 Telegram HTTPS responses, p95 625.4 ms, max 631.5 ms; 20/20 OpenAI HTTPS connections reached the endpoint and returned expected HTTP 401 without credentials, p95 490.1 ms, max 1,132.9 ms. No timeout occurred. This confirms the configured proxy route and TLS reachability at probe time; it does not prove that the historical 2/12 timeout ratio was caused solely by this WSL process.
- No direct fallback, proxy/VLESS credentials, OpenClaw config, Gateway, Windows portproxy entries, or unrelated services were changed. Remaining gap: transparent routing owner needs a WSL/kernel-compatible nftables redirect implementation or explicit operator decision; do not claim the full 40223->40222->VLESS chain is repaired from these probes alone.
- Follow-up owner proof found the live Gateway and its managed Codex children inherited `HTTP_PROXY/HTTPS_PROXY/ALL_PROXY=http://172.20.0.1:40223`, while Windows portproxy 40223 terminates at the WSL SSH route, not an HTTP proxy. An isolated route probe through that inherited endpoint produced OpenAI transport failures, confirming it is not a valid model proxy; `NO_PROXY` does not include Telegram or OpenAI hosts. The canonical OpenClaw proxy config resolves to the redacted VPS HTTP proxy on port 17890.
- Applied reversible service-owner repair: backed up both Gateway drop-ins with the same Luna timestamp suffix, then changed only their proxy environment values to the canonical configured proxy URL. Restarted only `openclaw-gateway.service`; it returned `active/running`, `NRestarts=0`, and readiness completed. Current Gateway `/proc` environment resolves all three proxy variables to the configured VPS host/port. No direct fallback was added.
- Current process-equivalent probes using the Gateway's exact inherited proxy: Telegram 20/20 responses, p95 619.7 ms, max 626.6 ms; OpenAI 20/20 reached the endpoint and returned expected HTTP 401 without credentials, p95 445.5 ms, max 483.3 ms; zero transport timeouts. `sing-box.service` remained `active/running` with unchanged restart counter 10,528 across a 12-second observation. Managed Codex child process was not active during the final snapshot; its next spawn must be checked for inherited proxy variables before declaring model-process acceptance.

## 2026-09-07 canonical Front admission owner (Luna)

- Replaced Front admission's raw `sessions.create` call with a core-owned `subagent.spawnVisible` seam backed by `maybeSpawnVisibleSession`. Native visible spawn registers requester session/origin, completion expectations and run lifecycle before Workboard accepts the execution.
- Added bounded post-await `sessions.describe` revalidation of the exact owner Telegram DM before `acceptExecutionLaunch`; stale, replaced or group sessions fail closed. The gateway bridge requires the in-process Telegram plugin owner and callback-time canonical Telegram owner resolution, preventing forged operator-write admissions.
- Removed caller-selected Gateway scopes from inline commit forwarding and added bounded unavailable reasons. Distinct inline result ids remain independent exactly-once tasks.
- Historical validation for the initial seam: Workboard 12/12, Telegram inline 7/7, plugin runtime 27/27. The current audit reran the affected core/extensions checks and current focused suites; no live deployment or Telegram-visible acceptance was performed.
- Remaining acceptance: fresh Front→native orchestrator→three depth-2 Luna worker trace, restart/replay, Companion projection and final exactly-once Telegram DM evidence. Live `maxSpawnDepth=2` was preserved.

## 2026-09-07 Front/inline authority and timing audit (Luna)

- Replaced the inline commit's local owner parser with the canonical Telegram `resolveTelegramCommandAuthorization` call immediately before chosen/callback commit. It honors numeric, `telegram:` and `tg:` owner entries, multiple owners, account identity, and current revocation. Channel `allowFrom:["*"]` remains DM access only; it does not grant owner authority. Workboard's gateway bridge uses the same canonical command authorization and requires the in-process Telegram plugin owner context; the public request has no caller-supplied `ownerAuthorized` boolean.
- Native visible child admission now aborts and deletes/reclaims the child if post-await Front-session revalidation or Workboard launch acceptance fails. Focused tests cover both rollback paths, plus the owner path, non-owner/group denial, account separation, multi-owner authorization, revocation, and forged gateway context.
- Added an inline-only answer envelope with an internal 9-second deadline and `inline_placeholder_deadline_exceeded` timing evidence. The existing 900ms authorization budget stays bounded; no global Telegram transport timeout changed. Tests cover a hanging post-confirmation commit and a hanging answer transport; the first placeholder is returned without model/Workboard/Orchestrator work.
- Acceptance targets are project requirements, not Telegram documented guarantees: placeholder p95 <=2s, hard internal envelope <=10s, and >=13s is FAIL. The 9-second implementation deadline leaves bounded margin under the 10-second target. `BOT_RESPONSE_TIMEOUT` and chosen feedback remain transport/runtime evidence to verify live; chosen feedback is statistical and cannot be treated as the sole functional commit path.
- Canonical proxy/VLESS transport remains injected at the Telegram runtime boundary; this slice adds no direct transport, fallback, retry, or proxy bypass. Worker admission continues through the existing native runtime/config path.
- Validation after this slice: Workboard Front admission 5/5, Workboard gateway 11/11, Telegram inline 13/13 plus runtime registration 1/1, and plugin runtime 28/28; core/extensions tsgo, targeted oxlint and oxfmt pass, plugin SDK public surface returned to its existing 4,355-export budget. No live deployment.

## 2026-09-07 inline transport and callback final-review hardening (Luna)

- The canonical `extensions/telegram/src/client-fetch.ts` transport now resolves `answerinlinequery` to a fixed 9,000 ms abort deadline before configured timeout values. The existing proxy/VLESS-backed fetch and shutdown/request cancellation remain the only transport path; no direct fallback or retry was added. `request-timeouts.test.ts` proves configured values cannot extend this method, and `bot.fetch-abort.test.ts` observes the underlying `AbortSignal` and timeout reason.
- Inline callback data is bounded by Telegram's 64-byte UTF-8 limit. Oversized or multibyte result ids use a deterministic 144-bit SHA-256 digest while the pending map stores the same bounded correlation id, preserving commit resolution. `bot-handlers.inline.test.ts` covers long multibyte ids and callback delivery.
- Repeated combined extension runs passed 3/3 (4 Telegram files: 51 tests; 2 Workboard files: 16 tests). The 9-file combined run passed 3/3 across agents (133), plugins (28), Telegram (51), and Workboard (16); the two runtime files were also repeated 3/3 (28 tests each). Core/extensions type checks, SDK surface (4,355 public exports), oxlint, oxfmt, and diff-check passed. No live deployment or Telegram-visible acceptance was performed.

## 2026-09-07 Front memory/responsiveness independent audit (Luna)

- The normal Telegram path authorizes and enqueues every message through `extensions/telegram/src/bot-handlers.inbound-pipeline.ts:173-250` and `extensions/telegram/src/bot-handlers.inbound-processing.ts:122-195,319-382`; no user-message-aware local Front disposition exists there. The only pre-model fast reply found is the native slash-command path in `src/auto-reply/reply/get-reply.ts:491-515`, whose implementation is limited to registered slash commands in `src/auto-reply/reply/get-reply-native-slash-fast-path.ts:69-105`. Greetings, small talk, and simple owner recall therefore still enter the ordinary model turn; the prompt policy at `src/agents/system-prompt.ts:535-554` cannot enforce a local answer boundary.
- Status and task commands are fast-paths, and `/stop` plus `/steer` have focused queue/abort behavior (`src/auto-reply/reply/get-reply-native-slash-fast-path.ts:205-329`, `src/auto-reply/reply/commands-session-abort.ts:130-205`, `src/auto-reply/reply/commands-steer.ts:27-53`, `src/auto-reply/reply/commands-tasks.ts:112-135`). They report session/task-registry state, not the Hicks Workboard aggregate; no source-owned bridge was found that makes `status/steer/cancel` operate on the active Workboard orchestrator run while preserving the Front sole-sender contract. This remains an implementation gap and is not accepted by the existing unit tests.
- Canonical memory is already loaded through the workspace bootstrap cache (`src/agents/bootstrap-cache.ts:44-67`) and bounded for `USER.md` (`src/agents/embedded-agent-helpers/bootstrap.ts:89-103,381-443`). Run-scoped memory prompt preparation is asynchronous (`src/plugins/memory-state.ts:259-320`, `src/agents/embedded-agent-runner/run/attempt-system-prompt-prepare.ts:178-205`), and repository bootstrap is a 2,000-character search-backed supplement (`src/agents/project-memory-bootstrap.ts:15-24,122-154`). These are valid model-context seams but not an O(1) pre-model Front profile/current-context answer. No dedicated canonical current-task summary exists in the live customization workspace; do not add a duplicate profile file or SQLite/JSON store.
- No hardcoded intent classifier was found in the current source. The typed `hicks_delegate` tool explicitly leaves the actionable decision to the model (`extensions/workboard/src/front-admission.ts:307-340`), which confirms the opposite gap: the current architecture has no deterministic local route for casual/fast recall and no source-owned pre-model admission result. A future Front seam should use a bounded, deterministic projection of already loaded canonical bootstrap facts, reserve `memory_search` for detailed recall, and return a typed disposition (`local`, `control`, `actionable`, `mixed`) before model/worker work.
- Existing read-only model-turn measurements remain 8.053s and 7.126s for name/timezone recall (recorded above); they measure the full model turn, not memory lookup. The independent focused regression run passed 7 files / 66 tests: bootstrap cache, project-memory bootstrap, native `/status`, `/tasks`, and `/steer`. This validates current helpers only; it does not prove the requested Front SLA.
- Required implementation/acceptance slice: (1) Front-owned pre-model disposition with no keyword classifier, using typed channel/owner facts and a bounded local resolver; (2) local greeting/simple profile answer with zero model, Workboard, worker, or broad memory-search calls; (3) Workboard-backed `status/steer/cancel` control projection with authoritative ownership and visible result under 2s; (4) mixed request sends the social acknowledgement immediately while one Workboard admission continues in background; (5) canonical Front-only completion delivery; (6) tests for cold/warm name/age/preference, correction+restart, long-term recall, control during active run, and deterministic caps/order. Until these exist and are live-measured, Front memory/responsiveness remains UNKNOWN.

## 2026-09-07 live acceptance harness preparation (Luna)

- Added `HICKS_ACCEPTANCE_RUNBOOK.md` with copy-ready, secret-safe gates for the full-bundle deploy transaction, Gateway readiness, proxy inheritance, native depth/overlap, Workboard/Companion projection, exactly-once completion, responsiveness, restart/replay, and Windows/browser/iCloud recovery. The runbook deliberately stops before Telegram-visible actions while the proxy policy is not PASS.
- The local deployment fixture remains the first executable gate: `node scripts/run-vitest.mjs test/scripts/hicks-deploy-runtime.test.ts` must pass all three cases (dry-run, successful full-bundle cutover, failed smoke rollback). Live deploy uses the existing `scripts/update-gateway.sh` delegation only after an exact clean source SHA and proxy PASS; no live deploy was run in this preparation.
- Proxy acceptance is now explicit: inspect redacted `HTTP_PROXY`, `HTTPS_PROXY`, and `ALL_PROXY` state in the Gateway PID and in the actual managed Codex app-server child PID, then run status-only Telegram/OpenAI probes. `../codex/codex-rs/app-server-daemon/src/backend/pid.rs` and `../codex/codex-rs/app-server-test-client/src/lib.rs` prove parent-environment inheritance, but a child PID check is still required. Current remediation evidence has Gateway inheritance PASS; a live managed Codex child was absent at the final snapshot, so child acceptance remains BLOCKED.
- The existing local Telegram scenario runner supports bounded send/click/API-hold/restart actions but has no inline-query/chosen-result driver. Inline p95 and the 9-second transport-abort target therefore remain explicitly BLOCKED until a real-user driver exists. No Test Server user-visible message, final DM, callback, or live restart was issued here.
- Synthetic native orchestrator evidence in the prior entry is reusable, but the remaining acceptance still requires a fresh Front depth-0 → `hicks-orchestrator` depth-1 → three overlapping native Luna depth-2 trace, Companion-visible Workboard cards/claims/terminal proof, one completion wake, one final DM, duplicate replay, and restart/recovery evidence. Current verdict remains UNKNOWN/BLOCKED by the named prerequisites rather than PASS.

## 2026-09-07 mandatory proxy policy implementation (Luna)

- Reused the existing managed proxy lifecycle and `proxy.enabled` routing seam; no new config surface or SQLite state was added. Telegram Front, polling, webhook, ingress worker, startup probe, target lookup, membership audit and cached client transport now carry the required-proxy policy when the Hicks managed proxy contour is active.
- Telegram required transport fails closed before any direct socket when the route is absent, env proxy selection would be bypassed by `NO_PROXY`, a caller supplies an opaque/direct fetch or dispatcher, or the proxy dispatcher cannot initialize. Generic calls without the required flag retain the existing direct/env fallback and sticky IPv4 behavior. Recovery is supported by rebuilding the transport after proxy lifecycle recovery.
- Model provider transport fails closed before guarded fetch when `OPENCLAW_PROXY_ACTIVE=1` but no env proxy route applies. Codex app-server spawn already rejects active proxy state without inherited route and preserves the inherited proxy environment for the child. This keeps Front/Orchestrator/native worker model requests and managed Codex children on the same route.
- Codex spawn environment now reasserts an inherited active marker after child overrides and rechecks the resulting proxy route, so a child cannot clear the marker and proxy variables together. Focused Codex transport coverage is 13/13.
- Regression evidence: Telegram fetch 53/53, probe 27/27, provider transport 2 workspace shards green; Codex transport/proxy lifecycle 50/50. Core and extension tsgo completed without diagnostics after the changes; formatting and `git diff --check` passed. No live deployment or Telegram-visible action was performed, so VPS health, managed child PID inheritance and end-to-end Front delivery remain live acceptance items.

## 2026-09-07 Front Workboard control bridge (Luna)

- Added a generic core session-action command seam. Only exact slash command names mapped by a loaded bundled plugin are intercepted; ordinary text and external plugin registrations fall through to existing command/model behavior. No natural-language, keyword, or length classifier was introduced.
- Workboard registers one `front-control` action for `/status`, `/tasks`, `/stop`, and `/steer`. The action requires the canonical owner/authenticated private Telegram Front context, matches the exact Front session binding, reads the `hicks-front` Workboard parent and child aggregate, steers the bound orchestrator with `sessions.steer`, and stops it with `sessions.abort` plus an authoritative Workboard block/clear of execution association. Front remains the only reply-producing surface.
- Front admission now persists the stable requester session binding in existing Workboard automation metadata; no SQLite schema or duplicate memory store was added. This makes control target lookup explicit instead of parsing notes or guessing from task registry state.
- Focused proof: plugin bridge 2/2 and Workboard owner/control 3/3; core and extension type checks passed; targeted formatter, oxlint and diff-check passed. No live deploy or Telegram send was performed.
- The bounded USER/bootstrap memory seam was left unchanged: existing cache and bootstrap caps are canonical, while a fast profile/current-task answer still lacks an owner boundary. Adding a Markdown parser or second store would violate the stated architecture, so that slice remains a named follow-up.

## 2026-09-07 mandatory proxy policy owner repair (Luna)

- Independent source review found that inherited proxy environment alone does not make managed Codex model traffic proxied: upstream Codex enables `RespectSystemProxy` only when `features.respect_system_proxy` is true (`../codex/codex-rs/core/src/config/mod.rs:1482-1490`), while its default is false (`../codex/codex-rs/features/src/lib.rs:1026-1033`).
- Repaired the managed app-server launch owner in `extensions/codex/src/app-server/transport-stdio.ts`: when `OPENCLAW_PROXY_ACTIVE=1`, launch args now enforce `-c features.respect_system_proxy=true`; the child marker and standard proxy route remain fail-closed. `OPENCLAW_PROXY_URL` alone is no longer mistaken for a Codex-consumed route.
- Added regression coverage for enforced app-server config and config-marker-only rejection in `extensions/codex/src/app-server/transport-stdio.test.ts`. Focused suite: 14/14; provider transport: 100/100; Telegram transport: 54/54. No live deploy or service restart in this source repair.
- Live review evidence: current Gateway process has canonical VPS `HTTP_PROXY/HTTPS_PROXY/ALL_PROXY` but `OPENCLAW_PROXY_ACTIVE` is absent. This is an owner/deployment blocker: current systemd environment must set the marker atomically with the explicit managed proxy (or invoke existing `startProxy`), then a real managed Codex child PID must prove both marker and route. Source repair does not claim live acceptance.
- Live marker repair completed afterward: backed up the current `40-codex-proxy.conf` drop-in with `*.bak-luna-marker-final-*`, added only `Environment=OPENCLAW_PROXY_ACTIVE=1`, reloaded systemd and restarted the owned `openclaw-gateway.service`. MainPID 233870 returned `active/running`; local readiness passed; `/proc` showed marker=true and all three standard proxy variables present on the canonical VPS route. Status-only probes through the exact inherited proxy returned Telegram HTTP 200 and OpenAI HTTP 401 (transport success), with no direct fallback evidence. A first managed-child smoke hit a transient SSH banner timeout before process evidence was collected; child marker/route inheritance remains unproven and must be rerun.
- Managed-child smoke rerun succeeded through the current Gateway: one observed Codex child PID (234342) inherited `OPENCLAW_PROXY_ACTIVE=1` and all standard proxy variables; the bounded model request returned the exact smoke marker, and stderr contained no proxy-failure indication. The process was allowed to exit and no service was restarted. This proves child env inheritance plus a successful model path through the configured proxy, but does not replace longer soak or Telegram-visible acceptance.

## 2026-09-07 independent pre-deploy review (Luna)

- Direct Codex contract review confirms app-server children inherit the parent environment: `../codex/codex-rs/app-server-daemon/src/backend/pid.rs:156-171` and `../codex/codex-rs/app-server-test-client/src/lib.rs:1538-1564` use `Command` with additive env overrides. OpenClaw now enforces `features.respect_system_proxy=true` when the managed marker is active and fails closed when no proxy route remains; the focused Codex/config suites passed 182/182.
- Telegram required transport and lifecycle evidence passed the focused 41 proxy tests and 35 Telegram runtime/probe/abort tests. Generic unmarked Telegram callers retain their existing direct behavior. Live proxy PASS is still absent because no fresh managed Codex child PID or outbound Telegram evidence was collected.
- Front control review is **FAIL**: generic `plugins.sessionAction` accepts an operator-write request, validates only plugin/action/schema/scope, then invokes the handler with caller payload (`src/gateway/server-methods/plugin-host-hooks.ts:102-234`). `extensions/workboard/src/front-control.ts:71-170` trusts payload `channel`, `senderIsOwner`, `isAuthorizedSender`, `agentId`, and `to`; a remote operator can therefore forge those fields and steer/abort a bound Hicks card. The local slash bridge is exact and its focused tests pass, but the privileged Gateway path needs a trusted owner-context capability or must be removed from this action before deploy.
- Deploy/reference sync is now covered by the owner transaction: `scripts/hicks-deploy-runtime.mjs:12-16,126-180,336-398` atomically stages `OWNER_POLICY.md`, `HICKS_ARCHITECTURE_PLAN.md`, and `WORKLOG.md` under the configured live workspace `hicks-reference/`, with rollback; `test/scripts/hicks-deploy-runtime.test.ts` passes 4/4 including the policy asset. The per-session normative contract remains the canonical `src/agents/system-prompt.ts` section; the full references stay out of the prompt. A fresh live workspace/session check is still BLOCKED because the configured Hicks SSH endpoint rejected the available key, and the historical `/home/clawd/.openclaw/workspace` must not be used as evidence.
- The bounded policy asset is 272 words / 1,973 bytes, and the runbook now checks the size, fresh-session contract, active workspace path, and reference-only handling. `HICKS_ACCEPTANCE_RUNBOOK.md` is formatted and `git diff --check` passes.
- Validation in this review: session-action contract 33/33, host-hook contract green, method scopes 174/174, proxy/env lifecycle 98/98, Codex startup/config 182/182, Telegram runtime/probe/abort 35/35, Workboard gateway/runtime 28/28, SDK surface unchanged at 4,355 public exports, and deploy fixture 4/4. Full core/extensions type and lint jobs were started but were still running behind shared artifact ownership at the review snapshot; no deploy or Telegram-visible action was performed.

## 2026-09-07 — authorized live full-bundle deploy and acceptance

- Local deploy fixture after legacy-artifact fix: 5/5 tests passed. The deploy owner now excludes nested `dist.pre-*`, `dist.backup-*`, `dist.failed-*`, and `.openclaw.{stage,backup,failed}-*` entries from the canonical staged install, moves them to the state backup area with a manifest after cutover, and restores them during rollback.
- Fresh live dry-run passed with Codex package contract `0.151.0` for `@openai/codex-linux-x64`; legacy backup staging fell from approximately 4.6 GiB to approximately 1.2 GiB. The first dry-run was safely terminated before cutover after confirming the live install and Gateway were untouched.
- Authorized atomic live deployment passed. Backup: `/usr/lib/node_modules/.openclaw.backup-20260907121839383`. Installed runtime manifest reports OpenClaw `2026.8.1` and Linux x64 Codex. Hicks reference manifest hashes matched the source for `OWNER_POLICY.md`, `HICKS_ARCHITECTURE_PLAN.md`, and `WORKLOG.md` at deployment time. Gateway restarted as PID `235959`; `/readyz`, deep Gateway status, and post-cutover Codex doctor were healthy.
- Post-deploy proxy proof: the live Gateway process inherited all six HTTP/HTTPS/ALL proxy variables, `NO_PROXY`, and `OPENCLAW_PROXY_ACTIVE=1`; the canonical systemd proxy drop-in remained present. Telegram polling account was connected and Bot API probe passed through the configured route. No user-visible Telegram message was sent during acceptance.
- Fresh main policy token probe passed for Front sole sender, Orchestrator/Workboard, native Luna depth 2, proxy-only, admin controls, and exactly-once completion wake.
- Synthetic Front-to-Orchestrator acceptance remains **BLOCKED**. A non-user-visible synthetic parent card was created and then moved to `blocked` after cleanup. A fresh `hicks-orchestrator` run used `gpt-5.6-luna` but loaded `/home/clawd/.openclaw/workspace-orchestrator` and reported that Workboard/card and native Luna dispatch tools were unavailable; therefore no new depth-2 fan-out, overlap, Workboard terminal proof, completion wake, Companion parity, or managed Codex child proof is claimed. Existing historical cards/sessions are not substituted for this fresh run.
- Post-deploy hardening (source only; no second live deploy): deploy paths now canonicalize existing ancestors before rejecting state/workspace roots equal to or below the install root. The updater requires `OPENCLAW_UPDATE_STATE_DIR` in full-bundle mode and passes it through before any Gateway stop. A stop followed by a pre-cutover rename failure restarts the old service and checks `systemctl is-active`; partial legacy archive restoration retains the recovery directory and reports its exact path when a restore fails.
- Hardening proof: deploy fixture `9/9` passed; updater fixture `33/33` passed; `bash -n scripts/update-gateway.sh`, `node --check scripts/hicks-deploy-runtime.mjs`, formatting, and diff checks passed. No second live deployment was attempted.
- 2026-09-07: Live root cause narrowed to optional Workboard discovery. The deployed plugin registers Workboard tools as optional; a broad `tools.profile: full` does not materialize them, and an agent `tools.allow` list is reported as unknown by the live doctor before plugin discovery. Added the versioned `customization/hicks/HICKS_ORCHESTRATOR_TOOL_POLICY.json` and made the deployment owner stage it beside the policy/plan/worklog. The canonical profile is `full` plus explicit Workboard `alsoAllow`, with no Telegram binding and `maxSpawnDepth: 2`; a fresh live inventory must show `hicks_delegate`, Workboard lifecycle tools, and native session tools before Front E2E is accepted.
- Follow-up hardening validation: after preserving partial archive metadata through outer rollback and including recovery paths in errors, deploy fixture remained 9/9 and root test typecheck passed. Updater fixture remained 33/33; targeted oxlint passed on retry after one transient boundary snapshot race. Shell syntax, Node syntax, formatting, and `git diff --check` passed. No live state was changed by this follow-up.

## 2026-09-07 live Orchestrator policy sync and acceptance checkpoint

- Narrow deploy/policy validation passed: Hicks deploy fixture **10/10**, combined policy-sync plus deploy fixture **12/12**; `node --check`, scoped format, and `git diff --check` passed. The deploy fixture covers symlink install-root rejection before staging, legacy archive exclusion/rollback preservation, service restart after pre-cutover rename failure, and nested state/workspace rejection.
- The live Orchestrator policy reference was backed up and updated atomically. The policy now explicitly denies the Front-only `hicks_delegate` while allowing the Workboard lifecycle tools; the live config backup was retained under the Hicks backup root. Policy application now preserves the config file owner across the atomic rename; a root-run attempt briefly produced an owner mismatch, was detected by Gateway EACCES logs, repaired before readiness, and covered by a regression assertion.
- Gateway restart/readiness after the repair is **PASS**: systemd `active`, `/readyz` returned `ready:true`, no failing readiness boundaries, and the config is owned by the service UID/GID with mode `600`.
- Fresh internal `hicks-orchestrator` inventory is **PASS** for Luna model, native `sessions_spawn`, `workboard_create`, `workboard_claim`, `workboard_decompose`, and `workboard_dispatch`; `hicks_delegate` is absent. The dedicated orchestrator workspace remains `/home/clawd/.openclaw/workspace-orchestrator` with bounded role files; canonical main policy/reference assets remain under `/home/clawd/.openclaw/obsidian-operating-base/hicks-reference/`.
- No arbitrary user-visible Telegram message was sent. Front depth-0 to Orchestrator depth-1 to three native Luna depth-2 workers, Workboard terminal evidence, completion wake, Companion parity, and final Telegram DM remain unclaimed pending the explicitly allowed owner-visible/manual acceptance lane.

## 2026-09-07 labeled owner Telegram acceptance attempt

- Existing authenticated Telegram client was available. One labeled DM `HICKS_ACCEPTANCE_LIVE_20260907_1721` was sent to Hicks Front, followed by one `/status` during the run. The Front UI visibly showed the acceptance message and returned `/status` with Gateway status; this is user-visible ingress/status evidence only.
- Acceptance stopped at the model boundary: Gateway journal recorded the Front run using configured `ollama/gemma4:e2b`, `fetch failed` / `LLM request failed: network connection error` after 882 ms, one retry, then a transcript-key error. The configured native Luna path was therefore not reached; no depth-2 workers, Workboard terminal proof, completion wake, or exactly-once final DM can be claimed.
- No inline query was sent after the failed labeled run, and no additional user-visible Telegram messages were sent. Exact blocker: live Front default provider/model is unreachable Ollama (`ollama/gemma4:e2b`); acceptance needs an owner-approved live model/proxy repair or explicit session model selection before rerun.

## 2026-09-07 live Front model repair and retry

- Live config comparison showed `main`, `agents.defaults`, `hicks-orchestrator`, and `luna` already set to `openai/gpt-5.6-luna`; the earlier Ollama failure came from a pinned Telegram session override (`ollama/gemma4:e2b`), not config drift. `/model default` visibly reset it to the configured OpenAI/Luna default, and `/new` visibly created a fresh session.
- Repair evidence: live config lists OpenAI auth profile `openai:lolkek238@icloud.com` without exposing credentials; Gateway process retains `HTTP_PROXY`, `HTTPS_PROXY`, `ALL_PROXY`, and `OPENCLAW_PROXY_ACTIVE=1`; `/readyz` and managed-Codex doctor both passed; bounded local Luna smoke returned `HICKS_MODEL_SMOKE_OK`.
- Fresh owner Telegram retry `HICKS_ACCEPTANCE_LIVE_20260907_1745` reached OpenAI/Luna. Front emitted one acceptance acknowledgement, `/status` during the run showed `openai/gpt-5.6-luna` and an active acceptance task, and logs recorded all three native worker markers `LUNA_ACCEPT_1`, `LUNA_ACCEPT_2`, and `LUNA_ACCEPT_3` with heartbeat/terminal-proof attempts. Exactly one final outbound DM was sent, but it reported **blocked**.
- Exact remaining blocker: Workboard rejected claim/lifecycle completion because the requester-owned parent card retained an unfinished dependency (`todo`); worker 1 also lacked an accessible terminal workspace, worker 2 was dependency-gated, and worker 3 produced passed proof but remained unable to complete. The three-worker/terminal/overlap acceptance is therefore not PASS, and no inline owner query was sent because the normal lane did not pass.

## 2026-09-07 inline UX checkpoint

- Inline placeholder already reads `Нажмите «Выполнить», чтобы запустить задачу`; callback acknowledges immediately, edits the inline message to accepted state, removes the Execute keyboard, and deduplicates callback/chosen confirmation.
- Targeted Telegram inline suite passed **14/14**. No deploy attempted because the live Hicks OpenAI profile is in cooldown and the current provider smoke cannot pass.

## 2026-09-07 Windows node/recovery checkpoint

- Live node inventory: Windows Headless is paired/approved/connected (OpenClaw 2026.8.1); MacBook Pro node is also connected. `nodes invoke system.which` reaches Windows node successfully, but returned no bins for the requested executables, so browser/Companion command execution is not yet proven.
- Windows has overlapping lifecycle owners: `OpenClaw Node` and `OpenClawNodeStable`, plus `OpenClaw WSL Anchor` and two `OpenClaw WSL Keepalive` task entries; iCloud browser also has separate stable launch ownership. No kill/restart was performed.
- Recovery acceptance (node kill, WSL/Gateway restart, network interruption, reboot, duplicate/stale fencing) remains unverified. No proxy bypass or live destructive action was attempted.
- Recovery owner audit: `OpenClaw Node` is a stale disabled `wscript node.vbs` task; `OpenClawNodeStable` is the active durable PowerShell node owner. Exported the stale task XML to the Windows recovery backup area and attempted to disable it; stable browser/node launchers were preserved. `OpenClaw WSL Anchor` and `OpenClaw WSL Keepalive` have distinct commands (anchor invokes the WSL keepalive binary; watchdog invokes the PowerShell watchdog), so they were retained.

## 2026-09-08 Luna read-only acceptance preflight

- Read-only Workboard/Front regression bundle: deployment fixture and policy-sync **12/12 PASS**; dispatcher ownership, gateway scope, CLI scope, lifecycle restart, and Front control suites passed. Combined command order was `dispatcher-ownership`, `lifecycle-sync.restart`, `front-admission`, `front-control`, `gateway`, `cli`, `hicks-deploy-runtime`, `hicks-orchestrator-policy-sync`; it completed **79/80 PASS** with one intermittent positive Front admission failure (`Workboard launch changed before native child acceptance` at `extensions/workboard/src/front-admission.ts:276`). Six bounded isolated reruns of `front-admission.test.ts` passed **5/5** each; full-shard repeatability remains required. Logs are retained under `/tmp/hicks-front-admission-flake-20260908/`.
- Workboard manifest inspection confirms all Hicks orchestration tools are declared, while `extensions/workboard/openclaw.plugin.json` remains `enabledByDefault: false`; live acceptance therefore requires proof that the configured runtime explicitly activates the plugin/slot. No staging/config/deployment mutation was performed.
- Direct Codex contract check covered `../codex/codex-rs/app-server-daemon/src/backend/pid.rs` (detached managed app-server launch and command environment) and `../codex/codex-rs/app-server-test-client/src/lib.rs` (stdio app-server handshake client). The Telegram E2E skill confirms Convex-leased Test Server TDLib is the real-user proof path; current local runner has no inline-query/chosen-result driver, so inline acceptance remains blocked until that driver exists.
- Controlled clock regression reproduced the CAS failure: `Date.now()` held at `1000` through create/prepare, then moved to `900` after native spawn; pre-fix `front-admission.ts:276` rejected `acceptedAt < preparedAt`. Repaired the Front owner boundary to reuse dispatcher monotonic acceptance (`Math.max(Date.now(), prepared.launch.preparedAt)`) and added a regression. Post-fix Workboard/Front suite passed **62/62**, targeted `oxlint` and `git diff --check` passed. Deployment Luna must rebuild and redeploy the changed artifact as part of the coherent bundle before live A/B.
- 2026-09-08 deployment owner checkpoint: verified `/tmp/hicks-final.tar.gz` and the Windows Temp copy byte-for-byte (70,064,244 bytes; SHA-256 `19b3667ee1bed4fadde8fa23a5a45f17cfa8df73c829f5e4fad167ec3f21f655`). Remote WSL/Gateway was reachable and `openclaw-gateway.service` remained active with the configured proxy environment and canonical `hicks-reference` present. The single atomic deployment transaction stopped at preflight before service stop/cutover because the archive omitted required `scripts/e2e/lib/codex-install-utils.mjs`, imported by `scripts/hicks-deploy-runtime.mjs`; live install and state were unchanged. No second transfer or live mutation was made. Regenerate the archive with that helper before retrying deployment.
- 2026-09-08 latest coherent Luna deployment: after the front-admission clock fix and `pnpm build:package` PASS (4m58.2s), built `/tmp/hicks-final-latest-20260908.tar.gz` (71,324,577 bytes; SHA-256 `b28bece89ef62695020d5e00013657e3cb2a3ba497d630fd6d67b5be919adaf1`). Windows certutil matched the hash; the payload included all deployment-owner helper imports. The second atomic transaction completed with backup `/usr/lib/node_modules/.openclaw.backup-20260908090909159` and reference backup `/home/clawd/.openclaw/obsidian-operating-base/.hicks-reference.backup-20260908090909162`; Codex 0.151.0 linux-x64 contract and bounded smoke passed. Fresh post-cutover evidence: systemd `active`, `/readyz` `ready:true`, event loop healthy, `/usr/bin/openclaw` resolves to `/usr/lib/node_modules/openclaw/openclaw.mjs`, and the service retains HTTP/HTTPS/ALL proxy plus `OPENCLAW_PROXY_ACTIVE=1`. Live Telegram/Companion and two real owner tasks remain acceptance gates.
- 2026-09-08 live A/B owner DM attempt after verified deployment: Telegram2 UI was explicitly inspected after root warning. ASCII owner requests `HICKS_ACCEPTANCE_AB_A Windows node recovery audit...` and `HICKS_ACCEPTANCE_AB_B Independent proxy connectivity audit...` were sent almost consecutively at 12:12. Front visibly acknowledged A with `Работаю` and `Принял аудит восстановления Windows-ноды...`; B was visibly acknowledged but immediately reported `пока заблокирован: у оркестратора уже выполняется активная Workboard-задача`. This proves both ingress and Front acknowledgements, but disproves second admission before first completion. No final A result or two final DMs were observed; Workboard/three-worker overlap and acceptance remain **FAIL/INCOMPLETE**. The live blocker is active-task admission contention/serialization, not service readiness or proxy proof.
- 2026-09-08 bounded owner-slot repair after live A/B: the blocker traced to `workboardCardSlotOwner()` treating every top-level requester-bound parent as the shared `hicks-orchestrator` slot. Requester-bound parent cards now use `${agentId}:${card.id}` for capacity while retaining `hicks-orchestrator` as claim authority; child placement slots are unchanged. Added a two-active-parent admission regression. Focused Front admission suite passed **7/7**; oxlint and `git diff --check` passed. Deployment Luna must rebuild/cut over this changed artifact before rerunning owner A/B; current live acceptance remains **FAIL/INCOMPLETE**.

## 2026-09-08 live Hicks runtime cleanup (Luna)

- After deployment owner release (Gateway PID `325241`, proxy route and readiness verified), created recoverable backup `/home/clawd/.openclaw/backups/cleanup-20260908-182836` with SHA-256 manifest, online backups of all nine agent stores plus Workboard/shared state, session archives, and config/systemd environment copies.
- Canonical cleanup archived 125 stale sessions; `sessions cleanup --all-agents --enforce --fix-missing` removed six missing main-store entries and 474 unreferenced old transcript artifacts. Four unarchived sessions remain only for the current main runtime handles; all other agents have zero unarchived sessions.
- Canonical Workboard card deletion removed all 68 cards and cascaded all child state: cards, boards, events, comments, proof, attempts, and worker logs are each **0**. The stale 24-hour running card had no linked task/session or live worker process and was removed after that check.
- Canonical `tasks maintenance --apply` found 502 terminal and 0 active tasks. After the owner retention check, the backed-up state DB was cleaned during a short Gateway stop in FK-safe order: task/subagent/flow runs, delivery queue/state, worker placements and histories, session heads/events/cursors, cron run receipts/scratch, and ACP replay/session bookkeeping are now 0. The Hicks proactive automation was disabled through the canonical cron owner; current main/deploy and system heartbeat handles remain preserved. No Telegram traffic was generated.
- Memory preservation proof: exact sorted `sha256sum` aggregate over `MEMORY.md` and `memory/**` remains `51e1deb2b387cda2d82daa9329b873574fab7c8e1451e49be7d6aec943e33eb3` across 197 files; main memory index rows remain sources 149, chunks 208, entry origins 155. Gateway `/readyz` is `ready:true`, service is active, and proxy environment remains present.
- Final reset proof after the second restart: archived session records were deleted through the canonical owner and 1,111 stale rollout transcript artifacts were removed while stopped; one fresh `agent:main:main` bootstrap handle is the only unarchived conversation because the owner forbids archiving an agent's main session. All ten stale thread-writer locks were removed; Gateway-owned tmp lock files were preserved. State and Workboard SQLite integrity checks remain `ok`, Gateway is `active` with `/readyz` `ready:true`, and proxy marker/environment remain set.

## 2026-09-08 runtime version and schema drift audit

- Source/package and the deployed bundle are `2026.8.1`. The live config's `meta.lastTouchedVersion=2026.8.2` is a future-config marker: `src/config/io.warnings.ts` emits the warning, while `src/config/future-version-guard.ts` blocks destructive config actions by an older binary. This is version drift in metadata, not proof that the live runtime should be changed by editing config.
- `audit-events-v2` is an independent shared SQLite schema migration. `src/state/openclaw-state-db-schema-repair.ts` detects a noncanonical `audit_events` shape and raises `OpenClawStateDatabaseSchemaMigrationRequiredError`; `src/state/openclaw-state-db-migration-required.ts` directs operators to `openclaw doctor --fix`. `src/state/openclaw-state-db-audit-migration.ts` performs a transactional table replacement and identity-key validation. No doctor, SQLite migration, config write, or schema mutation was performed because explicit approval is required.
- Direct sibling Codex contract inspection covered `../codex/codex-rs/app-server/README.md:31-37` and `../codex/codex-rs/app-server/README.md:74-85`: `/readyz` only proves listener acceptance and app-server clients require one initialize/initialized handshake. This supports retaining the existing Gateway readiness evidence without treating it as schema repair proof.
- Validation: `src/state/openclaw-state-db-schema-migration-required.test.ts` plus `src/state/openclaw-state-db.test.ts` passed **180 passed, 1 skipped**; config version/future-guard/io-compat suites passed **23/23**. Live owner WSL CLI was unreachable from this lane (Armbian was reachable but is not the Gateway owner), so no runtime deploy/restart or Companion cleanup was attempted here.

## 2026-09-08 Front typed routing and inline UX slice

- Source-only repair for the proven Front regression: `extensions/workboard/src/front-routing.ts` now classifies owner private Telegram turns as typed `local`, `control`, `actionable`, or `mixed`. A bounded process-local snapshot is preloaded from canonical `IDENTITY.md`/`USER.md` at Gateway start and refreshed on the next generation; message hot paths do not read files or call memory search.
- Local greeting/small-talk/basic profile recall returns a synthetic answer before the model and creates no Workboard card, worker, or search call. Actionable turns receive an immediate Front acknowledgement and automatic idempotent admission; mixed turns return their social acknowledgement while admission proceeds in the background. Reply context supplied by the channel is bounded into the canonical goal and the requester origin remains owner-bound.
- Telegram inline now uses the generic runtime `routeFront` seam. It returns one personal inline answer or acknowledgement under the existing 9s Bot API envelope and removes the `Выполнить` callback/chosen-result commit path. No live Telegram interaction or deployment was performed.
- Regression evidence: `extensions/workboard/src/front-routing.test.ts` passed **6/6**; `extensions/telegram/src/bot-handlers.inline.test.ts` passed **8/8**; Workboard gateway/admission/control passed **33/33**; runtime channel seam passed **10/10** and hook context passed **5/5**. Extension typecheck, extension lint, formatting, docs formatting, and `git diff --check` pass; pre-existing dirty checkout diagnostics remain outside this slice. Live inline query, automatic Workboard admission through the deployed Gateway, worker execution, and final owner DM remain **UNKNOWN** and require the Telegram E2E lane.

## 2026-09-08 inline Front boundary hardening (Luna)

- Inline model answers now use the same bounded Telegram deadline as Front routing, clear their timeout after completion, abort the pending provider call on expiry, and return a visible fallback when the provider rejects or exceeds the deadline. This closes the silent no-answer path while retaining automatic actionable admission and the no-button contract.
- Telegram message context now carries the selected reply id/body/sender into `ChannelContext.chat`, so the Front admission goal can preserve requests such as a reminder referring to a meeting message at the actual owner boundary. The body remains bounded by the Front goal cap.
- Regression evidence: `extensions/telegram/src/bot-handlers.inline.test.ts` passed **8/8** and `extensions/telegram/src/bot-message-context.reply-batch.test.ts` passed **6/6** after the changes. No live Telegram interaction or deployment was performed; deployed inline behavior and final owner DM remain **UNKNOWN** pending the Telegram E2E lane.

## 2026-09-08 inline Front hardening deployment (Luna)

- Rebuilt the exact dirty-checkout source bundle after the dedupe, admission, control-truthfulness, ingress-deadline, and reply-context repairs. Extension typecheck passed; targeted Front/Telegram/runtime suites passed **64/64**, **28/28**, and **37/37**; formatting and oxlint passed on the changed surfaces.
- Canonical owner transaction ran through `ssh hicks-companion` -> WSL with `/usr/bin/sudo`, using `scripts/hicks-deploy-runtime.mjs`, install root `/usr/lib/node_modules/openclaw`, workspace `/home/clawd/.openclaw/obsidian-operating-base`, and state root `/home/clawd/.openclaw`. It created backup `/usr/lib/node_modules/.openclaw.backup-20260908170914790` and reference backup `/home/clawd/.openclaw/obsidian-operating-base/.hicks-reference.backup-20260908170914794`; Codex 0.151.0 linux-x64 preflight and bounded smoke passed.
- Post-cutover proof: systemd `openclaw-gateway.service` is `active`; `/healthz`, `/startupz`, and `/readyz` are healthy (`ready:true`, event loop not degraded); installed `dist/telegram-ingress-drain-factory-duOOoGhm.js` SHA-256 is `456966f10c2921680d70bef4f28a8e99dba3007317c619a85a9b61e98f63126c`, matching the built source artifact and containing the visible inline fallback/control text. Gateway proxy variables are configured for port 17890; no managed Codex child existed during the passive check, so inherited child proxy remains unproven. Windows Headless node is paired/approved/connected.
- Browser CLI status is enabled but stopped with no tabs, so authenticated browser/iCloud usability remains **UNKNOWN**. No Telegram API send, owner DM, or Computer Use was performed. Passive capture directory is prepared at `/tmp/hicks-inline-proof-20260908-2010`; the live inline Telegram query and final owner-DM acceptance gate remain for the owner test window.

## 2026-09-08 passive inline acceptance failure

- The owner sent a second actionable inline test. Telegram polling received update ids `182010655` through `182010660` between 23:18:06 and 23:18:15 and advanced the offset after each fast-path delivery. Every corresponding lane failed within 1–2 ms with `Session changed before writer admission: agent:main:telegram:direct:351358741`.
- No `answerInlineQuery` success/error, inline timing, result payload, callback/chosen-result, new Workboard card, worker run, or final DM evidence appeared for this test. The owner reported that selecting/sending the inline result produced nothing. This is a live FAIL: the shared Telegram session is stale/rotated before Front writer admission, and the handler leaves no observable fallback answer in the deployed path.
- The earlier two cards remain blocked with Codex subscription usage-limit errors. `models status` still reports Luna OAuth `status: ok` and no unusable profile, so actual account quota must be treated as a separate execution gate. No Telegram action or recovery mutation was performed.
- Root cause traced and repaired locally: `runPluginEmbeddedAgentForResult` generated a random `sessionId` while reusing the existing Telegram `sessionKey`; writer admission correctly reread the durable row and rejected the mismatched generation before any provider call. The producer now reads the current durable session generation and passes a matching `sessionTarget` (falling back to a new id only when no row exists). Regression coverage passes **20/20**; extension typecheck, targeted lint, and diff checks pass. Live deployment remains pending.
- Owner feedback records ordinary Hicks Front response speed as **PASS (5/5)** for latency only. Execution efficiency, background completion, and final delivery remain unevaluated.

## 2026-09-08 final takeover verification

- Focused verification reran the changed Telegram and Workboard/runtime surfaces: Telegram **118/118**, Workboard/runtime **184/184**, extension typecheck, targeted oxlint, and `git diff --check` all passed. The previously flaky Front admission shard passed with the monotonic acceptance regression and concurrent-parent coverage.
- Read-only live checks through `ssh hicks-companion` -> WSL confirm `/usr/bin/openclaw` resolves to `/usr/lib/node_modules/openclaw/openclaw.mjs`, installed `telegram-ingress-drain-factory-duOOoGhm.js` retains SHA-256 `456966f10c2921680d70bef4f28a8e99dba3007317c619a85a9b61e98f63126c`, `openclaw-gateway.service` is active, `/readyz` is healthy with a non-degraded event loop, and HTTP/HTTPS/ALL proxy plus `OPENCLAW_PROXY_ACTIVE=1` remain configured.
- Live Telegram polling is connected and the Windows Headless node is paired/approved/connected. Browser status is enabled but stopped (`cdpReady=false`, no tabs), and the `hicks-front` Workboard board is empty after cleanup; Telegram-visible inline behavior, automatic admission, worker execution, and final owner DM remain **UNKNOWN** and are ready for the owner’s manual inline test window. No Telegram traffic, Computer Use, config, or SQLite mutation was performed.

## 2026-09-09 Guest Chat Mode Front migration (Luna)

- Telegram’s supported Guest Chat Mode is now the Hicks summoned-chat surface. The transport subscribes to `guest_message`, preserves the summoning message as Telegram’s separate user message, and answers exactly once with one generated `InlineQueryResultArticle` through `answerGuestQuery`; classic `inline_query` and `chosen_inline_result` are excluded from polling and the old front-routing classifier/route is removed.
- Guest turns use the ordinary Front model/system/memory/tool policy through the generic result-only runtime seam. Authenticated owner callers receive bounded instruction and reply context (text/caption, sender label, media type, and message reference); attachment file identifiers are excluded. The model’s typed `hicks_delegate` call remains the Workboard admission boundary, with final work delivery owned by the existing owner-DM path.
- Guest query ids are deduplicated with bounded in-flight retry release and terminal retention after Telegram accepts the answer. The result is HTML-escaped and bounded to Telegram’s message limit; ingress timing and abort signals enforce the existing short response deadline. `getMe`/probe capability reporting retains `supports_guest_queries`.
- Regression evidence: Guest handler, capability normalization, allowed-update filtering, ingress fast path, and sequentializer suites passed **85/85**; extension typecheck, targeted oxlint, and `git diff --check` passed. No Telegram send, Computer Use, config, or SQLite mutation was performed; live Guest Mode and owner-DM execution remain for the owner test window.

## 2026-09-09 Guest live-runtime cache/redeployment checkpoint

- The first post-activation owner query produced Telegram update `182010693`, but it followed the ordinary durable spool path and completed in 25 ms on lane `telegram:unknown`; no Guest handler, Front run, or `answerGuestQuery` call occurred. The completed ingress payload was tombstoned, so its exact non-Guest update type is no longer recoverable.
- A controlled cold restart changed Gateway PID `371764` to `383111`. Gateway readiness, Telegram polling, and the mandatory proxy environment recovered; config and both canonical memory hashes remained unchanged. Inspection then proved the installed `/usr/lib/node_modules/openclaw/dist` was still the pre-Guest bundle: no Guest handler or `answerGuestQuery` existed and the worker retained the old inline-query path. This deployment drift, rather than a process cache, is the immediate root cause.
- Current source Guest tests pass **23/23**, extension typecheck passes, and `pnpm build:package` completed successfully. The resulting bundle contains `guest_message`, the dedicated fast path, and `answerGuestQuery`; atomic live deployment and a new owner-run Guest test are the remaining gates.
- Atomic deployment from the current checkout completed through the canonical owner script, with installed-runtime backup `/usr/lib/node_modules/.openclaw.backup-20260909103341393`. The new Gateway PID `385529` is ready and Telegram polling is connected without errors. Loaded bundles contain the `guest_message` fast path and `answerGuestQuery`, while `inline_query`/`chosen_inline_result` are absent; loaded `allowed_updates` includes `guest_message`. Live bot capability reports `supports_guest_queries=true` and `supports_inline_queries=false`. Config, both memory files, and the mandatory proxy environment were preserved. Status: **READY_FOR_USER_GUEST_TEST**; no Telegram message was sent by Codex.

## 2026-09-09 Guest incoming identity repair

- The first live Guest update (`182010694`, owner text `TEST TEST TEST`) reached the dedicated handler but was silently discarded before Front/model execution. Root cause: `bot-handlers.guest.ts` read `guest_bot_caller_user` and `guest_bot_caller_chat`, fields describing messages sent by a guest bot, instead of the authoritative incoming `Message.from`, `Message.chat`, and `Message.guest_query_id` fields.
- The handler now owns the canonical incoming shape and records every non-admission as a bounded structured event with an enumerated reason and update id; message text, credentials, and query content are excluded. Regression coverage uses the real incoming shape and proves successful Front/`answerGuestQuery`, unauthorized caller rejection, missing query-id rejection, empty result recording, dedupe, and transient retry release.
- Verification: focused Guest test **6/6**, Guest/allowed-updates/probe/ingress/runtime suite **38/38**, extension typecheck, targeted oxlint, formatting, `git diff --check`, and full `pnpm build:package` pass. Built runtime owner: `dist/telegram-ingress-drain-factory-DxZpiMXm.js`.
- The initial deployment block was an stopped macOS Tailscale client. After restoring that route, the canonical `ssh hicks-companion` -> Windows -> Ubuntu-24.04 WSL path recovered. The superseding archive `/tmp/hicks-guest-ingress-20260909-2310.tar.gz` matched SHA-256 `e66f053f9dd043eff8dfce53e87c87cd1e85aed140eabf26d64c659544c03da2` on macOS, Windows, and WSL before extraction.
- Atomic deployment completed with runtime backup `/usr/lib/node_modules/.openclaw.backup-20260909201521444` and reference backup `/home/clawd/.openclaw/obsidian-operating-base/.hicks-reference.backup-20260909201521448`. Gateway cold-started from PID `2017` to `25681`; systemd is active and `/readyz` reports `ready:true` with a non-degraded event loop. Installed `dist/telegram-ingress-drain-factory-DxZpiMXm.js` exactly matches the built source SHA-256 `0c2d8df1b5361322fe4e236a45853842319fe29a1019972f88cf10d5fd5ee124` and contains `guest_message`, `answerGuestQuery`, and the structured non-admission reasons.
- Live Telegram probe is healthy and connected in polling mode with no error; `@hickss_bot` reports `supports_guest_queries=true` and `supports_inline_queries=false`. Config SHA-256, `MEMORY.md`, the aggregate long-term memory tree hash, and mandatory HTTP/HTTPS/ALL proxy markers match the pre-deploy values exactly. No Telegram message was sent. Status: **READY_FOR_USER_RETEST**; a fresh owner Guest query is the remaining visible E2E gate.
## 2026-09-10 Guest mode deploy-only redeployment

- 2026-09-10 redeployed the existing ready bundle via canonical `ssh hicks-companion` -> Windows -> `wsl.exe -d Ubuntu-24.04` and `scripts/hicks-deploy-runtime.mjs`; atomic cutover and cold restart completed, service `/readyz` healthy, loaded Guest response path present, and no `externalTurns.runResultOnly` path or Telegram traffic was used. Status: **DEPLOYED_FOR_USER_TEST**.

- Deployed the existing ready local bundle through `ssh hicks-companion` -> Windows -> `wsl.exe -d Ubuntu-24.04` using `scripts/hicks-deploy-runtime.mjs`; no source edits, rebuild, or Telegram message was performed. Runtime backup: `/usr/lib/node_modules/.openclaw.backup-20260909230334647`.
- Installed bundle proof: `guest_message` and `answerGuestQuery` are present in `dist/telegram-ingress-drain-factory-Bt0bzA_P.js`; classic `inline_query` and `chosen_inline_result` markers are absent. Gateway service is active and `/readyz` reports `ready:true`, non-degraded event loop. Telegram polling startup is present in the live journal.
- Status: **READY_FOR_USER_GUEST_TEST**. Raw capability and hash-preservation checks remain to be completed in the owner test window; no Telegram traffic was generated by Codex.
